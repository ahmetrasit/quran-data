=== PROMPT FRICTION ===

1. `Your response` bölümü üç parça isterken görev belgesi ve çıktı sözleşmesi ayrıca findings index'i zorunlu kılıyor. Orkestratör dört sahipli dosya verdiği için prose, evidence, index ve friction ayrı yazıldı.

2. Grounding kuralı, sonraki bir ayet okumayı açıyorsa o ayetin kelimesini tam `{ar, tr, gloss}` yüzeyiyle vermeyi istiyor. Hermetik iz ise sonraki ayetler için `source_ref + root + source_word_indices + role` sağlıyor, tam Arapça yüzey sağlamıyor; `inter_ayah_rows` da yalnız referans ve İngilizce not taşıyor. Bu nedenle bağlamsal paragraflar odak ayetin tam yüzeylerinden açıldı, sonraki ayetlerde yalnız bundle içinde açıkça verilen `ثَمَرَٰتٍ`, `تِجَٰرَةً` ve tam tekrar olduğu belirtilen `غَفُورٌ شَكُورٌ` yüzeyleri kullanıldı. Kalan bağlamlar ayet numarası ve sahneyle verildi. Tam yüzey şartı bu kaynak yapısıyla bütünüyle karşılanabilir değildi.

3. `word_analysis` on analitik/ortografik birim ve `35:30:1`-`35:30:10` hizaları taşıyor; QAC ise sekiz kelime ve 15 morfem taşıyor. Coverage, `35:30:9` ile `35:30:10` doğrudan kelime hizalarını güvenilmez ve dangling olarak işaretliyor. `word_morpheme_spans` on birimin tamamını sıfır atlamayla çözdüğü için konu kimlikleri aynen korundu, morfoloji iddialarında yalnız bu span çapraz geçidi ve QAC morfem referansları kullanıldı.

4. Hermetik `packet_summary.split_root_mappings` boş ve coverage içindeki Hermetik `split_root_count` sıfırdır. Buna karşılık `root_lexicon.root_mapping.split_roots_in_ayah` bir değerini verir, `ز ي د` için `root_000653` baskın olmayan hedefini kaydeder ve Hermetik `baseline_provisioned_arrival` modeli doğrudan `root_000653:B001` kullanır. Azık okumasında açık kök eşlemesi ile model kaydı esas alındı; çelişkili özet sayaç kanıt olarak kullanılmadı.

5. **Profil stili denetimi:** Prose üç açık olumsuz yüklem içerir. Bunlar artışı kimin istediğine ilişkin olası yanlış anlamayı düzeltmek, ilahî Şekûr'u ihtiyaç temelli insan minnettarlığından ayırmak ve sonraki olumsuz artış örnekleri biçimindeki karşı kanıtı korumak için bırakıldı. Hiçbir cümlede iki olumsuz üst üste yığılmadı; diğer karşıtlıklar olumlu yüklem ve `hem ... hem` yapılarıyla kuruldu.
