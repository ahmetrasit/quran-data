=== PROMPT FRICTION ===

- Üst görev dört ayrı dosya isterken inlined `Your response` bölümü üç parça sayıyor ve findings index'i bu listede anmıyor. Task document ile `COMMENTARY_SPEC.md` index'i zorunlu kıldığı, üst görev de dosya yolunu açıkça verdiği için dört dosyalı sözleşme uygulandı.
- `word_analysis` altı ortografik birim kullanırken QAC dört morfolojik kelime kullanıyor; upstream `aligned_qac_word_ref` alanlarında `35:19:5` ve `35:19:6` çözümsüz kalıyor. Prompt bu kusur için doğru çözümü de veriyor: yeniden numaralandırma yapılmadan `word_morpheme_spans.qac_refs` kullanıldı.
- Grounding kuralı, başka bir ayetin kelimesine dayanırken tam Arabic span ister. HFT'nin bazı context delta kayıtları yalnız ayet ref'i, kök, word index ve branch verir; tetikleyici kelimenin yüzeyini vermez. Bu nedenle tam yüzeyi inlined kanalda veya odakla aynı biçim kaydında bulunmayan bağlamsal deltalara bağımsız prose iddiası kurulmadı; eksilen ayrıntılar kanıt yüzeyinde açıklandı.
- Split-root metadata kendi içinde uyumsuzdur: `coverage.root_lexicon.root_mapping` bir split root bildirir ve HFT `root_001047/B010` outlier'ını kullanır; buna karşılık HFT packet summary `split_root_mappings: []`, coverage HFT kaydı da `split_root_count: 0` verir. Ayrıca coverage hedef normunu `ع م م` diye kaydederken `root_lexicon.root_001047.root_ar` alanı `ع م ي` gösterir. Prose kök iskeleti vermedi; evidence yalnız kararlı `root_001047/B010` kimliğini ve non-dominant split statüsünü kullandı.

Stil denetimi (sürtünme değildir): Prose'da iki açık olumsuz yüklem bırakıldı. "Eşit olmaz" ayetin birincil edimidir; "iki insanın bütün değeri değildir" ise bedensel durumdan bütünsel değer hiyerarşisi çıkarma yanılgısını düzeltir. Üçlük profil tavanı aşılmadı ve uzak dallar olumlu "benzetme düzeyinde kalır" diliyle sınırlandı.
