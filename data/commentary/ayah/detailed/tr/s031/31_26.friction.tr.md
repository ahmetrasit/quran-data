=== PROMPT FRICTION ===

- İstem içindeki “Your response” bölümü çıktıyı üç parça olarak sayıyor ve bulgular dizinini bu listede anmıyor; görev belgesi, çıktı sözleşmesi ve orkestratör ise dizini zorunlu dördüncü dosya olarak istiyor. Dört dosyalı açık sözleşme izlendi.
- Hermetic bağlam kaydı “şaşırtıcı uç okumaları koru” derken konu sistemindeki gibi zorunlu/aday ayrımı vermiyor. Üç `surprising_valid_outliers` okuması yerel kelimeye bağlanabildiği için düzyazıda korundu; diğer bağlam değişimleri ayrı okur kazancı ve yerellik ölçütleriyle seçildi veya kapsam notunda gerekçeli olarak bırakıldı.
- Bulgular dizini HFT okumaları için bir ad alanı tanımlamıyor. Yeni kimlik üretmemek amacıyla paketteki `model_id` ve `outlier_id` değerleri doğrudan ref olarak kullanıldı.
- Manifestlenen Hermetic paket yolu `latent_activation/...` biçiminde proje-bağıl veriliyor, fakat hangi depo köküne göre çözüleceği açıkça belirtilmiyor. Yol, kardeş proje kökü `/Volumes/OZTURK/_projects/latent_activation/...` altında çözüldü; yalnızca manifestte adı geçen tam dosya okundu.
- Kelime analizi 12 birim, QAC 10 kelime sayıyor ve ilk `lillâhi` içindeki ayrı Allah analizi için `word_morpheme_spans` çözümü yok. Paket bu kusuru bildiriyor fakat çözümsüz analitik birimin kanıt yüzeyinde nasıl gösterileceğini tarif etmiyor. Birleşik QAC kelimesinin `31:26:1:1`–`31:26:1:2` biçimbirimleri kullanıldı ve sınırlama kapsam notuna yazıldı.
- “Sonraki gelişmeleri en fazla üç paragrafta sıkıştır” kuralı, son yalın sentez paragrafının bu üç paragrafa dâhil olup olmadığını söylemiyor. Üç bağlam paragrafı kullanıldı; son paragraf yeni bağlam getirmeyen, odağa dönen sentez olarak ayrı tutuldu.

Stil denetimi: Düzyazıda üç açık olumsuz yüklem kullanıldı. `el-ganiyyü`nün sıradan servete indirgenmesini düzelten `yerleştirmez`, `el-hamîdü`nün insan övgüsüyle kazanıldığı yanılgısını düzelten `değildir` ve 31:27'nin kaynak ifadesini koruyan `tükenmezdi` profil tavanı içinde tutuldu; aynı cümlede iki olumsuz yığılmadı.
