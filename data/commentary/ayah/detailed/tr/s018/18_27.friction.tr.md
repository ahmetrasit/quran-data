=== PROMPT FRICTION ===

1. Task belgesi “bundle v12 reader responses içeriyorsa kullan” derken V2 profil, varsayılan iş akışında bu yanıtların emekliye ayrılmış olabileceğini özellikle söylüyor. Bundle’ın üst düzey `v12_reader_responses` değeri boş nesne, `coverage.v12_reader_responses.present` ise `false`; coverage belirleyici kabul edildi ve aşamalı/yalıtılmış okuma iddiası kurulmadı.

2. Aynı durum `v12_focus_trace_hermetic` için de var: üst düzey alan boş nesne, coverage hem `present:false` hem `packet_present:false` diyor. Bu yüzden profile rağmen `baseline_models`, `context_deltas` veya `surprising_valid_outliers` üretilemedi; düzenli ve geniş reader-walk ile çapraz-koşu bulguları kullanıldı.

3. QAC âyeti on beş ortografik sözcük içinde biçimbirimlere ayırırken word-analysis `و`, `لِ` ve benzeri bağlı parçaları ayrı analitik birimler sayarak on sekiz konu adresi veriyor. Yeni bir ortak numaralama icat edilmedi; evidence yüzeyinde iki kimlik sistemi ayrı tutuldu.

4. Açık olumsuz yüklemler âyetin iki doğrudan inkârını aktarmak ve `ك ل م` yara eşseslisinin yerel “sözler” anlamını devralmasını önlemek için kullanıldı. Bunlar canlı ana anlam ve replacement riskleriydi; diğer karşıtlıklar olumlu yüklemlerle kuruldu ve varsayılan üç olumsuzluk tavanı aşılmadı.
