=== PROMPT FRICTION ===

1. `word_analysis` uses 13 analytic words while QAC supplies 11 morphological words; the bundle itself marks the alignment inconsistent and warns against repairing the dangling refs `18:7:12` and `18:7:13`. I used `word_morpheme_spans` as instructed and cited the actual QAC morphemes.
2. The `ب ل و` root mapping names a secondary split target (`root_000154`) whose dictionary payload is omitted, while the Hermetic Focus Trace that could carry secondary split-root detail is absent. I kept the word-analysis-approved yıpranma/yenilenme coloring and did not add unsupported secondary branches.
3. The task document calls candidates discretionary, while the 5.6 Sol High profile requires every candidate whose payoff is not already expressed. I treated the rendering profile as the stricter run-specific rule and covered all distinct candidate payoffs.

Two explicit negative predicates were retained in the prose. One protects the central reading from the likely misconception that attractive appearance itself certifies value; the other clarifies that `أَيُّهُمْ` is an embedded selector rather than a direct audience question. No other specification rule had to be invented.
