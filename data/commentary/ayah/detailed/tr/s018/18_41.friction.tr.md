=== PROMPT FRICTION ===

1. The prompt requires every nonduplicative candidate payoff to be included, while several candidates are low-level sound observations that overlap strongly with grammar or closure topics. I treated them as integrated sentence-level payoffs and mapped each candidate explicitly in the evidence surface rather than giving each one separate prose.

2. The bundle reports nine analytic word units but QAC has eight orthographic words because `فَ` and `لَن` are separate analytic units inside one QAC word. It also marks the upstream `aligned_qac_word_ref` sequence as inconsistent. I followed `word_morpheme_spans` for actual morphology references while preserving the upstream topic IDs unchanged.

3. The word-analysis record for `لَهُ` displays a `w-l-y` root field, while QAC identifies the surface as a preposition plus pronoun with no lexical root. I retained the candidate’s reader payoff only as syntactic relation and target-directedness, and did not claim that `لَهُ` lexically derives from that root.

4. The task says to state when both staged reader responses and Hermetic Focus Trace are absent, while the structure rule says source absence belongs in the coverage note and never in prose. I placed that statement in the evidence coverage note.

5. There was live replacement risk and counter-pressure requiring explicit correction: the morning branch could replace copular “becoming,” `لَن` could be overstated as absolute eternity, “gavran” could be flattened into annihilation, and a common Turkish rendering could shift `طَلَبًا` from seeking to finding. These were contained in the prose and evidence surface. No additional specification conflict or invented substantive rule was needed.
