=== PROMPT FRICTION ===

The bundle requires the writer to say when both `v12_reader_responses` and `v12_focus_trace_hermetic` are absent, while the structure rule says all source absence belongs in the coverage note and never in prose. I resolved this by reporting both absences only in the evidence file’s coverage note.

The bundle explicitly reports an upstream identity defect: 18 analytic word records, 15 QAC words, only five resolved `word_morpheme_spans`, and 13 unreliable `aligned_qac_word_ref` values. Exact cross-layer phrase joins are therefore impossible for most of the ayah. I did not renumber or repair them; the evidence surface cites word-analysis topic IDs and QAC morpheme refs independently.

No `18_71` outputs existed. The prompt specifies three parts but not repository filenames; I inferred the versioned prose/evidence/friction filenames from the requested directory and the instantiated profile name. No other rule had to be invented.
