=== PROMPT FRICTION ===

1. `Your response` bölümü çıktıyı üç parça diye sayıyor ve findings index’i bu sayımda anmıyor; governing output contract ise ayrı bir findings index’i zorunlu kılıyor. Orchestrator dört ayrı dosya istediği için index’i dördüncü artefakt olarak yazdım.

2. `91:9:4:form-v-contrast` zorunlu konusu 87:14’teki Form V karşılaştırmasını prose’a taşımayı gerektiriyor. Grounding kuralı başka ayetin kelimesi için tam structured span isterken kompakt bundle 87:14’ün Arapça yüzeyini açık bir alanda vermiyor; yalnız referansı ve Form V bilgisini veriyor. `{ar:تزكى, tr:tezekkâ, gloss:arındı}` yüzeyini bu morfolojik kayıttan kurdum ve evidence yüzeyinde `[inference]` olarak işaretledim.

3. Normal reader walk `root_000637/B005` tek-çift dalını etkinleştirirken cross-run publication aynı okumayı `reject` olarak işaretliyor. Candidate nomination ile son reconciled publication arasındaki öncelik açık bir tek cümleyle tanımlanmamıştı. `word_analysis` bu dala topic vermediği ve publication reddettiği için okumayı prose/index dışında, coverage reddi olarak korudum.

Style audit: Prose’da iki açık olumsuz düzeltme kullandım. Biri çiftçilik basıncının yerel `eflaha` anlamının yerini almasını, diğeri geçişli arındırma fiilinin sözlü öz-temize çıkarmayla karışmasını önlüyor. İkisi de profilin izin verdiği misconception/replacement-risk gerekçesine giriyor; hiçbir cümlede iki olumsuz üst üste binmiyor ve toplam, üçlük tavanın altında.
