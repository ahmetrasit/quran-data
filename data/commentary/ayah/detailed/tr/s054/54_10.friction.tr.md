=== PROMPT FRICTION ===

Run mostly clean. Two constraints required careful handling rather than a new rule: the prompt asks to include candidate topics with distinct payoff, while also keeping this short ayah from becoming bulky; I compressed overlapping candidate payoffs into connected paragraphs and documented coverage in the evidence surface. The channel material names parent/subchannel labels, but the governing rules forbid treating first-pass review as an established channel ledger, so I used it only as a local word-to-word connection and marked that as inference.

No live misconception, replacement risk, or counter-evidence required more than the few explicit negative predicates used to protect local sense: especially that `ٱنتَصِرْ` is broader than simple "help me" and that channel review is not an adjudicated ledger. `v12_reader_responses` and `v12_focus_trace_hermetic` were absent, so I reported absence in coverage and did not infer staged or Hermetic before/after data.
