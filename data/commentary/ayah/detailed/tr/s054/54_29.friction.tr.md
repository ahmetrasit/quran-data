=== PROMPT FRICTION ===

Run mostly clean.

Two minor frictions:

- The prompt says to include every candidate with a reader payoff not already expressed, while also asking for increased precision without extra bulk. I handled this by merging overlapping candidate payoffs into the nearest local-word paragraph instead of giving each candidate a separate sentence.
- The bundle records unreliable word_analysis-to-QAC word alignment because QAC counts four morphological words and word_analysis counts seven analytic words. I followed the bundle's instruction to use `word_morpheme_spans` for joins.

I revised the prose to avoid explicit negative predicates; there was no live misconception, replacement risk, or counter-evidence requiring one after the collective-agency point was rendered positively.
