=== PROMPT FRICTION ===

Run mostly clean.

One small specification tension remains: the general output contract says there are two artifacts, prose and evidence surface, while the instantiated task requires a third friction artifact. I followed the later, explicit three-file instruction.

The prompt allows reading channel-generated external files when channel detail is necessary, but this ayah has no anchored channel subchannels and no adjudicated ledger. I treated those files as unnecessary and did not read them.

The prompt asked for before/after reader value, but both `v12_reader_responses` and `v12_focus_trace_hermetic` are empty. I used the regular and wide reader walks plus cross-run publication only where they added retrospective insight, and reported the missing staged/trace sources in the evidence coverage note.
