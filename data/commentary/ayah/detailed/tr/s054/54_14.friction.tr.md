=== PROMPT FRICTION ===

The run was mostly clean.

One tension remains: the task document says that if both staged reader responses and Hermetic Focus Trace are absent, the absence should be said; the same prompt also says absences belong in the coverage note, never in prose. I followed the latter rule and reported the absence in the evidence coverage note.

The prompt permits reading `channel_generated_outputs` files when channel-family detail is necessary, but the user instructed work only in this repository and the inlined bundle already supplied the needed first-pass channel constraints. I did not read external quran-data files.

The bundle's QAC word count and word-analysis word count conflict by design for this ayah. I used the bundle's own `word_morpheme_spans` policy and did not invent corrected word ids.
