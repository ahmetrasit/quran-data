=== PROMPT FRICTION ===

- Rendering profile, Hermetic Focus Trace içindeki `surprising_valid_outliers` okumalarını odakta köklenmiş kaldıkları ölçüde korumayı isterken grounding kuralı, daha sonraki bir âyetin kelimesine dayanıldığında o kelimenin tam Arapça yüzeyini şart koşuyor. Bundle, `O_poisoned_sky_conduit`, `O_body_axis_tree` ve `O_anti_vine_of_honor` için gerekli bağlam köklerini ve Türkçe rollerini veriyor fakat tam kelime yüzeylerini vermiyor. Bu üç outlier'ı yüzey uydurmadan prose taşımak mümkün değildi; evidence kapsam notunda açıkça kaydedildiler. `O_tree_eater_reversal` yalnız odak âyetin ağaç ve yutma alanlarıyla zeminlenebildiği için taşındı.
- Aynı yüzey eksikliği `D_judgment_bridle`, `D_counter_orchard`, `D_failed_kinship_support`, `D_grafted_foreign_lineage` ve `D_allotted_decision_tree` context delta okumalarını da etkiledi. Bazı modeller ayrıca kolayca bütün-sure sistemi anlatımına genişliyor ve Layer 2'nin kanal/sure mimarisi yasağıyla gerilim oluşturuyor. Yerel etkisi odak kelimelerden ya da bundle içinde yüzeyi verilen 44:44-45 kelimelerinden kurulabilen beş delta taşındı; diğer beşi coverage kaydı olarak bırakıldı.
- Task document birleşik yanıtta üç parça sayıyor, fakat genel output contract findings index'i dördüncü ayah çıktısı olarak zorunlu kılıyor ve orchestrator dört ayrı canonical dosya istiyor. Daha özel orchestrator talimatı izlenerek prose, evidence, index ve friction ayrı yazıldı.
- `candidate` kabul ölçütü “başka yerde ifade edilmemiş ayrı okur getirisi” olarak veriliyor, fakat birbirine yakın mercy-boundary/mercy-counterpoint ve marked-formula/rare-formula getirilerinin ne zaman aynı sayılacağı mekanik olarak tanımlanmıyor. Bu çalışmada kelimeye özgü işlevleri ayrı olduğu için 12 candidate konunun tamamı kabul edildi; prose içinde birleştirildi, indexte ayrı `topic_id` satırları korundu.
- Cross-run publication çene ve destek-frame okumalarını `reject` sayarken Hermetic Focus Trace aynı okumaları geçerli exploratory baseline/context modelleri olarak tutuyor. Profile cross-run verisinin yerel bundle kanıtını geçersiz kılmamasını söylediği için bu basınçlar açık ağaç anlamını değiştirmeyecek containment cümlesiyle prose taşındı ve `reject` notu evidence yüzeyinde karşı-delil olarak korundu.

Style audit: Prose içinde bir açık olumsuz yüklem, çene/mızrak/düzenek basıncının somut ağaç anlamını ikame etmesini önlemek için tutuldu (`yeniden adlandırmaz`). Bu canlı replacement riski nedeniyle gerekliydi, üçlük tavanın altındadır ve aynı cümlede ikinci bir olumsuzlukla yığılmamıştır.
