=== PROMPT FRICTION ===

- Upstream identity friction: QAC records seven orthographic words, while `word_analysis` records eight analytic words and leaves `44:33:8` dangling. The bundle explicitly forbids repair by renumbering. I used `word_morpheme_spans` for morphology joins and preserved every upstream topic ID unchanged.
- Output-contract friction: the final inlined response instruction asks for three parts and omits the findings index, while the governing output contract and the orchestrator require four separate files. I followed the four-file orchestrator contract and emitted prose, evidence, index, and friction.
- Candidate-policy ambiguity: the base task calls candidates discretionary, while the rendering profile says to include every candidate whose payoff is not already expressed. I admitted all 25 candidates because each retained a distinct local grammar, sound, boundary, lexical, or contextual formulation after integration.
- Focus-trace scope ambiguity: the profile says to use `baseline_models`, `context_deltas`, and `surprising_valid_outliers`, but does not say that every context delta must become prose. Rendering all 14 deltas would conflict with the isolated-ayah rule and the instruction to compress later developments. I used all four baselines and all four valid outliers, admitted seven context deltas with distinct local payoff, and documented the seven non-independent or surah-dependent deltas in evidence coverage.
- Grounding friction for outliers: several outliers cite later roots and word positions without supplying those later Arabic surfaces in the inlined trace. I grounded all four outliers through this ayah’s own fully spanned words and rendered them as contained, distant inferences rather than importing unsupplied neighboring wording.
- Channel-status limitation: the available review is first-pass and single-reader, with no adjudicated ledger or maturity state. I used its local selection-to-testing anchor only as corroboration and made no channel, recurrence, or whole-surah claim.
- Style audit: the prose contains one explicit negative contrast, used to prevent the likely grammatical misconception that `مُّبِينٌ` modifies the signs rather than `بَلَاءٌ`. No other live misconception, replacement risk, or counter-evidence required an explicit negative predicate.
