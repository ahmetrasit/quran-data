=== PROMPT FRICTION ===

The run was otherwise clean, but two specification pressures required explicit handling:

1. The bundle’s word analysis counts 13 analytic/orthographic units while QAC counts 12 words, and upstream `aligned_qac_word_ref` values after `وَ` become unreliable. I followed the bundle’s own instruction to use `word_morpheme_spans` and did not renumber identities.
2. The task requires carrying layer-1 and layer-3 exclusions, but the bundle contains no separate `consideredNotPrimary` or surah-exclusion artifact. I reported the absence in coverage and invented no exclusions.

The prose uses two explicit negative predicates to protect live counter-evidence: restriction is not arbitrary deprivation, and neither abundance nor scarcity is a verdict of human worth. Both are supported by the bundle’s 89:15-16 counter-reading and reader-walk synthesis. No other prompt rule had to be invented.
