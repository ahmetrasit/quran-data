=== PROMPT FRICTION ===

- **Parça sayısı çelişkisi:** Son "Your response" bölümü yanıtı üç parça diye tanımlarken görev belgesi ve output contract ayrı bir findings index de zorunlu kılıyor. Kullanıcının açık dört dosyalık talimatı bu çelişkiyi çözdü; dört dosya üretildi.
- **Kimlik hizalama çelişkisi:** `word_analysis` 21 analitik birim kullanıyor, QAC 17 ortografik kelime kullanıyor ve `aligned_qac_word_ref` alanlarının dördü QAC'de karşılıksız kalıyor. Bundle bu alanın güvenilmez olduğunu da açıkça söylüyor. Konu kimlikleri aynen korundu; QAC dayanakları `word_morpheme_spans.qac_refs` üzerinden verildi.
- **Split-root sayısı çelişkisi:** `coverage.v12_focus_trace_hermetic.split_root_count` değeri `0`, buna karşılık `coverage.root_lexicon.root_mapping.split_roots_in_ayah` değeri `2` ve `ء م و` ile `و س ع` için ikişer hedef kayıtlı. Split-root yorumlarında daha ayrıntılı ve dalları fiilen içeren `root_lexicon` kaydı esas alındı; HFT outlier sınırları korundu.
- **"Use" kapsamı belirsizliği:** Rendering profile, HFT'nin `baseline_models`, `context_deltas` ve `surprising_valid_outliers` alanlarını "kullan" diyor fakat her kaydın prose'a girmesini mi, yoksa ayırt edici getiri denetiminden geçmesini mi istediğini belirtmiyor. Aynı profil sonraki gelişmeleri en çok üç paragrafla sınırlıyor. Dört baseline model ve üç outlier korundu; altı deltadan yerel okumaya ayrı getiri ekleyen dördü alındı, iki yinelenen delta coverage notunda kaydedildi.
- **Topraklama ile dış paket kullanımı:** Yağmur-aralık outlier'ı 24:43 kelimelerine dayanıyordu, ancak inlined HFT özetinde o kelimelerin tam yüzeyleri yoktu. Bundle'ların açıkça adlandırdığı tek `packet_source_file` okundu ve yalnız 24:33 ile 24:43'te gereken tam yüzeyler kullanıldı. Bu, "explicit source manifest" izninin HFT `packet_source_file` alanını kapsadığı yorumu ile yapıldı.
- **Eksik kaynaklar:** Normal per-ayah okuyucu cevapları emekliye ayrılmış ve yoktur. Normal okuyucu yürüyüşü dosyası bulunmuş fakat başlıklar ayrıştırılamamıştır. Bütüncül sure okuması yoktur. Geniş pencere yürüyüşü, HFT ve çapraz-koşu yayını bulunduğu için canlı talimatlar uygulanabildi.
- **Stil denetimi:** Prose'da iki açık olumsuz yüklem kullanıldı. Biri split-root annelik alanını `imâikum`un yerel anlamı sanma riskini, diğeri çoğul evlendirme emrini herkese derhal evlilik dayatması sanma riskini düzeltiyor. İkisi de profilin izin verdiği canlı yanlış-anlama/yer-değiştirme istisnası kapsamındadır ve varsayılan üç olumsuzluk tavanının altındadır.
