=== PROMPT FRICTION ===

1. “Your response” bölümü çıktıyı üç parça diye tanımlayıp prose, evidence ve friction sayıyor; aynı promptun output contract'ı bulgular dizinini dördüncü zorunlu artefakt olarak istiyor. Orkestratörün açık dört-dosya talebi ve ayrıntılı output contract esas alındı.

2. `word_analysis` 13 analitik/ortografik birim kullanırken QAC 10 morfolojik kelime kullanıyor. Coverage bunu tutarsız sayıyor ve `24:44:11`, `24:44:12`, `24:44:13` reflerini dangling olarak kaydediyor. Topic kimlikleri yeniden numaralandırılmadı; QAC iddiaları yalnız `word_morpheme_spans` içindeki gerçek morfem reflerine bağlandı.

3. Hermetic coverage `split_root_count: 0` ve boş `split_root_mappings` bildirirken `coverage.root_lexicon.root_mapping.split_roots_in_ayah: 1` diyor ve `ء و ل` için `root_000067` ile `root_001684` hedeflerini açıkça listeliyor. Ayrıntılı root-lexicon eşlemesi ve rendering profilinin aykırı split-root okumalarını koruma talimatı esas alındı. Çelişen özet nedeniyle bu okuma prose içinde biçimce uzak, ikincil kelime oyunu olarak sınırlandı.

4. Hard-grounding kuralı, bağlamdan gelen her okuma için sonraki/başka âyet kelimesinin tam Arabic span'ını istiyor. İnline Hermetic kayıtlar ilgili âyet reflerini ve kökleri veriyor, fakat tam yüzey biçimlerini vermiyor. Bundle'ın `text.source` alanında tam yolu açıkça adlandırılan Quran text dosyası yalnız gerekli âyet satırları için okundu; bu alanın “explicit source manifest” sayılması promptta örnek verilen `files[]` kadar biçimsel olarak tanımlanmamıştı.

5. “Ayrı okuyucu getirisi” ölçütü, 32 adayın bir kısmında zorunlu konularla neredeyse aynı sonucu veriyor. Uygulanan karar kuralı şuydu: ayrı gramatik, sözcüksel, ses/form veya bağlamsal sonuç üreten aday alındı; yalnız aynı sonucu başka kaynakla tekrarlayan ya da surah çapında sistem gerektiren aday evidence içindeki red kaydında tutuldu.

6. Hermetic profil baseline modelleri, altı context delta'yı ve iki outlier'ı “use” diyor; ana görev ise bağlam malzemesini yalnız ayrı getiri sağladığında almayı ve sonraki gelişmeleri en çok üç paragrafa sıkıştırmayı istiyor. Dört ayrı getiri sağlayan delta ve iki outlier prose alındı. `delta-transformative-hinge` ile `delta-privacy-clock`, inter-ayah karşı satırları ve Layer 3 sınırı nedeniyle evidence yüzeyinde gerekçeli olarak bırakıldı.

**Stil denetimi:** Prose üç açık olumsuzluk içeriyor. 24:11'den verilen tam span içindeki “sanmayın” kaynak âyetin kendi yasağıdır. “Silmez” bedensel görüşün içgörü tarafından silinmesini, “yerine geçmez” ise uzak split-root yankısının yerel “sahipler” anlamını değiştirmesini önleyen iki replacement-risk korumasıdır. Üçü profile ait tavana uyar ve aynı cümlede iki olumsuz yığılmamıştır.
