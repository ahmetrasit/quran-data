=== PROMPT FRICTION ===

1. `word_analysis` 13 ortografik/analitik birim kullanırken QAC 11 morfolojik kelime kullanıyor. `aligned_qac_word_ref` alanında `24:5:12` ve `24:5:13` çözümsüz kalıyor; ayrıca `وَأَصْلَحُوا` ile `فَإِنَّ` iki sistemde farklı bölünüyor. Prompt bu kusuru bildirip `word_morpheme_spans` kullanımını emrettiği için topic kimliklerini korudum ve kanıt yüzeyindeki QAC bağlarını morfem aralıklarından kurdum.

2. Kapsam bloğu `v12_focus_trace_hermetic.split_root_count: 0` ve packet özeti `split_root_mappings: []` diyor; buna karşılık `root_lexicon` aynı âyet kökünü `split` olarak işaretleyip `root_000172` hedefini taşıyor ve `base_new_stability_after_rupture` modeli doğrudan bu hedefi etkinleştiriyor. Bu kaynak içi çelişkide modeli atmadım; okumayı ikincil ayrık hedef olarak, yerel tövbe anlamını açıkça koruyarak ve `[inference]` işaretiyle taşıdım.

3. Profil, bağlamla açılan her okumada sonraki âyetin kelimesi için tam Arapça surface span istiyor. Focus Trace'in bazı sonraki-âyet izleri yalnızca `source_ref + root + source_word_indices` ve İngilizce rol açıklaması veriyor; 24:35 ve 24:61 aykırı okumalarında tam yüzey metni yok. İki aykırı okumayı koruma talimatıyla grounding talimatını birlikte uygulayabilmek için izde kimliği belirlenen kelimelerin asgarî Kur'an yüzeylerini dosya ya da dış kaynak okumadan yeniden kurdum. Bu, promptun tanımlamadığı bir normalizasyon kuralıdır.

4. `v12_reader_walks` boş nesne olarak gömülmüşken coverage bloğu bunu `present: false` ve “başlıklar ayrıştırılamadı” notuyla açıklıyor; buna karşılık `v12_reader_walks_wide` mevcut. Coverage kaydını belirleyici saydım, düzenli yürüyüşü eksik ve geniş yürüyüşü mevcut olarak raporladım.

Stil denetimi: Prose içinde bir açık olumsuz yüklem kullanıldı: “Bu ikincil basınç yerel anlamın yerine geçmez.” Bu kullanım, split-root okumasının yerel `tâbû = tövbe ederek dönmek` anlamını ikame etme riskini gidermek için tutuldu. Başka açık olumsuz yüklem kullanılmadı; karşıtlıklar olumlu yüklemlerle kuruldu.
