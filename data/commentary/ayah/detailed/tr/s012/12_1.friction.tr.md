=== PROMPT FRICTION ===

1. Genel görev belgesi `candidate` konularını takdire bağlı sayarken 5.6 Sol High profili, getirisi başka yerde ifade edilmemiş her `candidate` konusunu dahil etmeyi istiyor. Daha özel rendering profilini uyguladım; yinelenen getirileri tek sentez cümlesinde birleştirdim.

2. Görev, iki ayrı aygıt kaynağı da yoksa bunun söylenmesini isterken eksikliklerin düzyazıya değil coverage notuna konmasını da emrediyor. `v12_reader_responses` ve `v12_focus_trace_hermetic` yokluğunu yalnız evidence dosyasındaki kapsam notunda belirttim.

3. Evidence yüzeyinin zorunlu alanları tarif ediliyor, fakat dosya şeması verilmemiş. Adreslenebilir `E01`–`E23` kimlikleri, iddia türü ve paket referansları içeren Markdown tablosunu çalışma kuralı olarak seçtim.

4. `v12_cross_run_publication.baseline.target_tokens` eşlemesi QAC kelime kimlikleriyle kaymış görünüyor: örneğin “Râ” `12:1:2`ye, “Bunlar” `12:1:3`e bağlanmış; buna karşılık `word_morpheme_spans` ve QAC kayıtları `الٓر` için `12:1:1`, `تِلْكَ` için `12:1:2`, `ءَايَٰتُ` için `12:1:3` veriyor. Cross-run token eşlemesini hiçbir sözdizimsel iddiada kullanmadım; publication bulgularını yalnız coverage/priority kontrolü olarak kullandım.

5. Bir açık olumsuz yüklem, `الٓر`nin olağan sözlük kelimesi sanılması ihtimalini düzeltmek için kullanıldı (“olağan bir sözlük kelimesi gibi işlemez”). Ayrıca “iki ayrı seçenek değildir” ifadesi, Türkçedeki “âyet” ile “işaret/delil” ayrışmasının yerel anlam alanını bölmesini önlemek için kullanıldı. Bunların dışında canlı bir yanlış anlama, yerel anlamı değiştirme riski veya karşı-delil ek olumsuzlama gerektirmedi.
