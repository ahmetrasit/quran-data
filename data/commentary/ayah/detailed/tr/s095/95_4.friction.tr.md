=== PROMPT FRICTION ===

- Bundle içindeki `word_analysis.words[].aligned_qac_word_ref` dizisi yedi analitik kelime varsayıyor, QAC ise altı kelime ve dokuz morfem veriyor. Coverage bu join’i açıkça “UNRELIABLE” sayıyor; buna rağmen topic kimlikleri aynı kaymış 1-7 dizisini kullanıyor. Çalışmada topic kimlikleri yüzey sırasıyla, morfoloji ise doğrudan QAC morfem kimlikleriyle izlendi.
- `word_morpheme_spans` son üç kritik yüzeyi çözemiyor. QAC’de `فِىٓ`, word-analysis’te `فِي` yazımı gibi ortografik farkların normalleştirilip normalleştirilemeyeceği belirtilmemiş. Kimlik uydurmamak için otomatik eşleme yapılmadı.
- `95:4:4:social-perceptive-human` topic gerekçesi V4’te `ء ن س` için kök satırı bulunmadığını söylüyor; aynı bundle’ın fallback inventory ve `root_lexicon.root_000059` alanları bu kök için tam dallar içeriyor. Daha yeni ve typed bundle alanları açıklama desteği olarak kullanıldı.
- `95:4:6:beauty-goodness-function` topic’i `ح س ن:B003` dalını dayanak gösteriyor. Fallback inventory bu kökte yalnız `B001`, `B002`, `B004`, `B005` verirken tam `root_lexicon.root_000323` kaydı `B003`ü de içeriyor. Prose iddiası ortak bulunan `B001` ve `B002` ile sınırlandı.
- `COMMENTARY_SPEC.md`, eski birleşik Layer 3 + 2.5 akışının emekli olduğunu söylüyor; `docs/CHANNELS.md` ise birleşik üretimi etkin iş akışı olarak anlatıyor. Bu âyet çıktısı kanal iddiası kurmadığı için çatışma sonucu etkilemedi.
- “Your response” bölümü üç parça sayıyor ve findings index’i listede anmıyor; üstteki output contract ile orchestrator isteği dört ayrı dosya gerektiriyor. Dört dosyalı açık orchestrator isteği izlendi.
- Aday konular için “ayrı reader payoff” eşiği mekanik olarak tanımlanmamış. Örtüşen adayların dışarıda bırakılma kararları evidence coverage notunda tek tek kaydedildi.

Stil denetimi: Prose içinde bir açık olumsuz yüklem kullanıldı. Bu, Türkçedeki “takvim” kelimesinin yerleşik takvim/cetvel anlamını yerel Arapça mastar anlamından ayıran canlı yanlış-anlama riskini düzeltir; profil tavanı olan üçün altında kalır ve aynı cümlede ikinci olumsuz yoktur.
