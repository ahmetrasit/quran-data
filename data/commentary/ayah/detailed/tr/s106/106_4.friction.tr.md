=== PROMPT FRICTION ===

Editoryal geçiş, ilk geçişteki hiçbir evidentiary judgment, anchor, kabul, dışlama veya karşı kanıt kararını değiştirmedi. Yalnızca tekrarları sıkıştırdı, yakın mekanizmaları aynı paragrafta topladı ve evidence/index landing adreslerini `EP01`-`EP18` olarak yeniden kurdu. İlk geçişte index/prose'da bulunan `secure-town-inversion` topic'i evidence surface'te açık bir topic ref olmadan kalmıştı; bu çapraz-dosya tutarsızlığı mevcut `QI-9c4ad103` kaydıyla burada onarıldı. Dört ilk-geçiş dosyası değiştirilmedi.

- Hermetik prompt, dış dosya erişimini genel olarak kapatırken `channel_generated_outputs` manifestini gerekli olduğunda belirli quran-data dosyaları için izinli sayıyor. İnlined channel ve HFT malzemesi yeterli olduğundan ek channel dosyasına başvurulmadı.
- `word_analysis` 8 orthographic unit, QAC ise 7 morphological word sayıyor ve `106:4:8` dangling ref bildiriyor. Bu upstream hizalama kusuru korunmuş; numaralandırma değiştirilmemiş, word-level eşleştirmede `word_morpheme_spans` ve morfem referansları kullanılmıştır.
- `v12_reader_responses` yoktur; bundle bunu emekliye ayrılmış varsayılan iş akışı olarak bildirir. Editoryal metne stage, isolated-response veya confidence hareketi eklenmedi.
- `v12_focus_trace_hermetic` reconstructed before/after sinyalidir. Baseline, context delta ve outlier kimlikleri evidence'da korunmuş; staged transcript, reader convergence veya maturity iddiasına dönüştürülmemiştir.
- Channel review `first-pass-single-reader` durumundadır. Channel bağlantıları yerel adaylama ve yazar sentezi olarak tutulmuş; kabul, ret, maturity veya sûre-geneli channel iddiası üretilmemiştir.
- `channel_generated_outputs` içinde `paths/path_families/semantic_path_families.jsonl.gz` missing_expected_files olarak kayıtlıdır. Uncompressed semantic path family manifestte görünse de bu ayah için zorunlu bir kaynak değildir.
- `ء م ن` için V4 root data eksiktir. QAC biçimi, `مِّنْ خَوْفٍ` tamamlayıcısı ve retained lexical evidence güvenlik okumasını destekler; trust/faith basıncı narrowed olarak korunmuştur.
- Cross-run publication içindeki iki `reject` canlı tutulmuştur: graft'ın yolculuk kollarını yerel düzene aşılaması prose'a nedensel iddia olarak alınmamış, `o_grafted_collective` yalnızca maddî benzetme olarak sınırlandırılmıştır; güvenliğin otomatik iman veya onay doğurması da prose'a geçirilmemiştir.
- Korkunun azaltma/aşındırma dalı, yerel korku anlamının yerine geçirilmeden imge olarak; graft dalı da besleme fiilinin yerine geçirilmeden benzetme olarak korunmuştur. Bu editoryal sürümde tek açık olumsuz yüklem, güvenlikten kendiliğinden iman sonucu çıkarılmasını sınırlamak için gereklidir.

Density audit

- `must_integrate` topics: **18**; her biri findings index'te bir kez ve editorial prose'da identifiable landing ile yer alıyor.
- Admitted `candidate` topics: **27**; her biri findings index'te bir kez ve editorial prose'da taşınıyor.
- `ledger_only` topics: **0**.
- Distinct local resonances: **17**; 11'i `[supports-primary]`, 6'sı `[shifts-primary]` ilişkisiyle kaydedildi.
- Prose resonance landings: **17**; her resonance için ayrı bir reader payoff korunuyor.
- Shared landings: **6**. `EP10` systemic viability + seasonal resilience + split-growth; `EP11` mobile corridor + route-kept-on-course + covenantal safe-conduct; `EP12` social composition + sustaining completion; `EP13` worship-enabled-response + level-ground-for-agency; `EP14` house-refuge-larder + protective-container; `EP16` inner/outer reanimation + release-of-constriction aynı yakın mekanizma içinde ayrı cümlelerle işlendi. Her grupta mekanizma ve okuyucu kazanımı ayrı kalıyor.
- Required bundle item missing: **yok**. Kayıtlı eksikler optional/retired veya known-upstream-boundary niteliğindedir.
