=== PROMPT FRICTION ===

- **Friction — output count:** Task document and `COMMENTARY_SPEC.md` require prose, evidence, findings index, and friction. The instantiated “Your response” section says “three parts” and omits the findings index from its numbered list. The user explicitly requested four canonical files, so the four-output contract was followed.
- **Friction — word identity:** `word_analysis` has nine analytic surfaces while QAC has seven morphological words; upstream refs `110:3:8` and `110:3:9` therefore do not resolve. No identities were repaired or renumbered. `word_morpheme_spans` was used for every morphology join, while topic IDs were preserved verbatim.
- **Friction — formula grounding:** Several candidate topics cite related praise-command formulas, especially 15:98, but the bundle's inter-ayah rows provide references and notes rather than the other ayahs' exact Arabic surfaces. The prose therefore states only the locally visible, generic formula relation and does not quote or describe an ungrounded other-ayah continuation.
- **Friction — return branches:** The scoped branch inventory exposes only `ت و ب B003` (“calling another to return”), while full `root_lexicon` contains `root_000189/B002`, the divine acceptance and turning sense required by the local word analysis. The local sense was grounded in B002; B003 was retained only as an explicitly inferred invitation resonance.
- **Style audit — explicit negatives:** The prose uses three explicit negative predicates, matching the profile ceiling. They protect three live replacement risks: sound as semantic proof, `كَانَ` as an expired past, and divine `تَوَّابًا` as human remorse. Other contrasts were rendered through positive predication.
