=== PROMPT FRICTION ===

- Friction: Grounding kuralı, başka bir âyet okumayı görünür kıldığında o âyetin ilgili kelimesini tam `{ar, tr, gloss}` spanıyla vermeyi zorunlu tutuyor. Compacted word-analysis, HFT, reader-walk ve inter-ayah kayıtları cross-ayah referans, kök, kelime indeksi veya rol veriyor fakat exact Arabic surface vermiyor. Hermetik sınırı korumak için başka dosya okumadım ve surface uydurmadım; P4 ve P13–P15 bu referanslara 5:58'in kendi tam spanlarından giriyor. Bu nedenle literal cross-ayah word-span şartı karşılanamadı.
- Friction: `word_analysis` 15 analitik kelimeyi `5:58:1`–`5:58:15` diye adlandırırken QAC 12 orthographic word kullanıyor ve son üç upstream ref dangling kalıyor. Bundle bunu bilinen defect olarak işaretliyor. Evidence yüzeyinde topic kimliklerini korudum, morphology joinleri için `word_morpheme_spans.qac_refs` kullandım ve kimlikleri yeniden numaralandırmadım.
- Friction: Branch kimlikleri root-scoped olmasına rağmen `branch_inventories.full_context_packet` bazı split kökleri tek raw-root başlığı altında birleştiriyor; HFT ile `root_lexicon` ise `root_id/branch_id` kullanıyor. Özellikle `ن د و`, `ص ل و`, `ه ز ء` ve `ل ع ب` dal numaraları bu yüzeylerde bire bir aynı envanteri göstermiyor. Evidence'da HFT/root-lexicon iddialarını daima `root_id/branch_id` ile yazmayı ilerleme kuralı olarak seçtim.
- Friction: HFT coverage `split_root_count: 0` derken root-lexicon coverage bu âyette iki split QAC kökü bildiriyor ve kinetic outlier açıkça non-dominant `root_001563/B001` kullanıyor. Outlierın kendi activation trace'i ile root-lexicon mappingini controlling evidence kabul ettim.
- Friction: Profil iki baseline, beş context delta ve iki outlierı kullanmayı isterken sonraki gelişmeleri en çok üç paragrafa sıkıştırıyor. Beş deltayı P13–P15'te üç hareket altında birleştirdim; iki outlierı 5:58'in kendi kelime alanlarından açılan P11–P12'de tuttum.
- Friction: Adayın “ayrı reader payoff” taşıyıp taşımadığı için mekanik bir ölçüt verilmemiş. `5:58:12:prayer-identity-background`, zorunlu `5:58:5:surah-prayer-thread` ile aynı 5:12/5:55 payoffunu tekrarladığı için dışarıda bırakıldı; diğer 20 aday etkinleştirildi.
- Style audit: Prose üç açık Türkçe olumsuz yapı kullanır: açılıştaki `akletmeyen` temel anlamı taşır, thermal outlierdaki `yüklemez` primary sense replacement riskini sınırlar, kapanıştaki `kuramaması` âyetin causal diagnosisini yeniden söyler. İki olumsuzluk aynı cümlede yığılmamıştır.
