=== PROMPT FRICTION ===

- Çıktı sayısı konusunda metin kendi içinde ayrışıyor: son “Your response” bölümü üç parça sayıp findings indexi anmıyor, görev belgesi indexi zorunlu kılıyor. Orkestratörün açık dört-dosya isteği ve üstteki output contract izlenerek index ayrı dosya olarak yazıldı.
- Word-analysis 15 analitik yüzey kullanırken QAC 12 morfolojik kelime kullanıyor; `5:37:13`, `5:37:14` ve `5:37:15` doğrudan QAC kelime referansı olarak sarkıyor. Bundle bu kusur için `word_morpheme_spans` kullanımını açıkça söylediğinden yeni kimlik icat edilmedi ve bütün bağlar morfem spanları üzerinden kuruldu.
- Split-root kaydı çelişkili: `v12_focus_trace_hermetic.packet_summary.split_root_mappings` boş ve coverage `split_root_count: 0` diyor; buna karşılık `reader_hft_a/O2_FAILED_RETRACTION_FROM_SPLIT_ROOT` açıkça ikincil `root_000555` eşlemesini kullanıyor ve `root_lexicon` aynı hedefi `non_dominant_split_target` olarak veriyor. Profil aykırı split-root okumalarını korumayı özellikle istediği için O2, root lexiconun bağımsız desteğiyle ikincil ve `[inference]` işaretli olarak korundu.
- Dal kapsamlarının sayıları iki kaynak ailesinde aynı değil: fallback branch inventory `ق و م` için 20, `ن و ر` ve `ع ذ ب` için 8’er dal bildirirken tam `root_lexicon` sırasıyla 21, 11 ve 9 dictionary dalı bildiriyor. Profilin “root_lexicon tamdır” hükmü izlendi; kullanılan dallar kimlikleriyle iki tarafta da çözülebildiği ölçüde kanıtlandı.
- HFT’nin O3 okuması yerel `النار` ve `مقيم`e bağlı olsa da 5:55’teki namaz/ateş teması kolayca yerel anlamın yerine geçirilebilir. Okuma, 5:55’in kendi `يقيمون الصلاة` yüzeyi verilerek ve namazı ateşle özdeşleştirmeyi açıkça reddeden bir koruma cümlesiyle sınırlandı.
- Stil denetimi: Prose içinde iki açık olumsuz yüklem bırakıldı. “Onlar ondan çıkanlar değildir” âyetin kategorik ana hükmünü koruyor; “Bu, namazı ateşle özdeşleştirmez” ise O3’ün canlı yer-değiştirme riskini düzeltiyor. İki olumsuz aynı cümlede yığılmadı ve profil tavanı olan üçün altında kalındı.
