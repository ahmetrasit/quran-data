=== PROMPT FRICTION ===

- The user requested exactly three files, while the commentary spec also requires a findings index "alongside" prose/evidence/friction. I placed the findings index inside the evidence file to obey the three-file constraint.
- The embedded bundle marks `word_morpheme_spans` as mostly unresolved and word-analysis/QAC alignment as inconsistent. I kept word-analysis topic refs and QAC morpheme refs distinct rather than repairing the alignment.
- The Hermetic Focus Trace requires use of later context, but several later-context deltas provide later ayah refs, roots, and word indices without Arabic word surfaces in the embedded prompt. Because the prompt also requires later-ayah grounding through visible word spans, I compressed those deltas and entered them through focus-ayah words instead of giving unsupported later Arabic spans.
- `ش ي ء` has no Turkish `root_lexicon` dictionary/gloss entry, while other bundle families use it materially. I used the branch inventory, reader walks, HFT, and cross-run publication for its "knowable thing" role and recorded the lexicon gap in coverage.
- Profile-specific style audit: the prose keeps one explicit corrective negative predicate, "geciken karşılık da gözden kaçma anlamına gelmez", because HFT `c_delay_not_heedlessness` requires protecting delayed response from being read as inattention. Other contrast-heavy sentences were revised into positive predication.
