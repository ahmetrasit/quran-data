=== PROMPT FRICTION ===

- The prompt requires a findings index, but the user provided only three writable paths: prose, evidence, and friction. I placed the findings index inside the evidence file after the evidence surface and coverage note.
- The prompt says the instantiated prompt is self-contained and should be read as the ayah-specific source, while the embedded bundle is a single very large JSON line that is impractical to inspect through terminal output without truncation. I used the named bundle file `bundles/s041/41_8.ayah.json` for structured `jq` inspection of the same source data and did not read unrelated repository material.
- The profile says to use Hermetic Focus Trace baseline models, context deltas, and surprising outliers, while also requiring at least two-thirds of this short ayah’s prose to remain on its own wording and later developments to be compressed. I admitted the context material that added distinct reader payoff, compressed it into the final later-context paragraphs, and mapped additional overlap-heavy context deltas in evidence rather than expanding prose for each one.
- The bundle includes first-pass channel review and generated channel coverage, but Layer 2 may not name or establish a surah-wide channel. I used those rows only as local supporting evidence in the findings index and avoided channel names in the prose.
- Style audit: explicit negative predicates were kept only where the ayah’s own `غَيْرُ مَمْنُونٍۢ` construction and the likely misconception about “kesintisiz ödül” required them. No extra corrective negation was added for stylistic contrast.
