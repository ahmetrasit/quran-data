=== PROMPT FRICTION ===

- The task contract requires prose, evidence surface, findings index, and friction as separate outputs, but the user constrained writing to exactly three files: prose, evidence, and friction. I placed the findings index inside the evidence file to satisfy the findings requirement without creating a fourth file.
- The prompt says the full prompt file is self-contained and should be read. The embedded bundle is a single very large JSON line; I used `jq` extraction on that embedded line instead of dumping the whole line into the conversation. No external bundle, prompt, script, or other ayah output was read.
- The bundle reports `word_analysis_qac_alignment.consistent=false`: word_analysis uses 24 orthographic units while QAC has 19 morphological words. I treated word_analysis `topic_id`s as stable commentary refs and used `word_morpheme_spans` for QAC evidence on the closing units rather than renumbering.
- Several HFT context deltas depend on later or neighboring ayahs, but the prose-grounding rule requires a later ayah word to be shown with its own full span before leaning on it. The compact HFT data often gives roots and word indices without the Arabic surface word. I therefore kept later-context material compressed and grounded it through this ayah's own words, with fuller provenance in evidence rather than importing unsupported later-word spans into prose.
- The qirāʾāt/variant details for `نُذِيقَهُمْ` and `نَّحِسَاتٍ` are mentioned in word_analysis, but the compact prompt did not expose full variant readings. I preserved only the scoped claim that variants affect immediate agency or sound while the local frame remains stable.
- Style audit: explicit negation was kept below the profile ceiling and used only where the ayah itself carries negation or where a likely misconception needed containment, especially the final `لَا يُنصَرُونَ`.
