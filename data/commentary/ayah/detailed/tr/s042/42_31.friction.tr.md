=== PROMPT FRICTION ===

- Output contract friction: the prompt asks for prose, evidence surface, findings index, and friction, while the user explicitly allowed writing only three paths and named no separate findings-index file. I placed the findings index inside the evidence file after the coverage note, matching the local S42 output convention and respecting the three-file constraint.
- Prompt/source friction: the prompt says it is self-contained and not to read other files, but the embedded bundle is a very large single JSON block that terminal output truncates. I read the full task/spec sections from the prompt and queried the exact named bundle file `bundles/s042/42_31.ayah.json` for ayah-specific fields so the required topic ledger and evidence could be covered without browsing the repository generally.
- Alignment friction: the bundle reports `word_analysis` has 18 orthographic units while QAC has 14 morphological words, with direct word-level joins marked unreliable. I used `word_morpheme_spans` and QAC morpheme refs for Arabic surfaces and morphology rather than silently correcting upstream identities.
- Evidence friction: HFT later-context deltas and outliers provide refs, roots, branch IDs, roles, and changed readings, but often not full Arabic surfaces for later-ayah words. I cited later context by reference and grounded the prose in this ayah's own words instead of inventing later Arabic spans.
- Lexicon friction: `root_001684` (`و ل ي`) has dictionary branches but no reviewed Turkish gloss in `root_lexicon.coverage`; the prose uses the included dictionary branch definitions and records the missing gloss in coverage.
- Channel scope friction: two channel subchannels are anchored at 42:31, but the bundle labels channel review as first-pass discovery rather than an adjudicated channel ledger or maturity plan. I used them only as local candidate support already grounded in `مُعْجِزِينَ`, `وَلِىٍّ`, and `نَصِيرٍ`, and did not name or assert a surah-wide channel.
- Coverage limitation: `v12_reader_responses` are absent under the retired per-ayah workflow, and no whole-surah reading exists for S42. HFT, regular reader walk, wide reader walk, cross-run publication, inter-ayah rows, root lexicon, and branch inventories are present and used within their stated limits.
- Style audit: the prose uses explicit negation heavily because the ayah itself is built from repeated negation. I kept additional correctional negation for live misconceptions only: pardon as escapability, `veliyy` as mere friendship, and legitimate human agency as erased by the final support denial.
