=== PROMPT FRICTION ===

- The user requested exactly three output files, while the prompt contract asks for prose, evidence surface, findings index, and friction. I followed the existing local pattern by placing the findings index inside the evidence file and reserving this file for prompt friction.
- The instantiated prompt says the file is self-contained and not to read outside it, but the embedded bundle is a huge one-line JSON block that exceeded terminal output limits. I queried the named `bundles/s042/42_16.ayah.json` file by key to recover the same bundle data without truncation; no unrelated source files were read.
- The bundle coverage reports that word-analysis word refs do not align cleanly with QAC word count for this ayah. I therefore cited QAC morphology at morpheme level and treated word-analysis topics by their own topic ids rather than fabricating corrected word joins.
- Later-context HFT rows cite later ayah refs, roots, and word indices, but do not provide later Arabic surface forms. The prose used Turkish concept labels for those later-context effects rather than inventing Arabic spans.
- Style audit: the prose avoids avoidable explicit negation; the remaining contrast is carried mostly by positive predication and containment rather than by correction formulas.
