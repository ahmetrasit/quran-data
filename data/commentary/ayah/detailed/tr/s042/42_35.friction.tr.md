=== PROMPT FRICTION ===

- Output contract friction: the prompt asks for prose, evidence surface, findings index, and friction, while the user explicitly allowed writing only three paths and named no separate findings-index file. I placed the findings index inside the evidence file after the coverage note, following the existing S42 output convention and respecting the three-file write constraint.
- Prompt/source friction: the prompt says it is self-contained and not to read other files, but the embedded bundle is a very large single JSON block that the terminal truncates. I read the full task/spec sections from the prompt and queried the exact named bundle file `bundles/s042/42_35.ayah.json` for ayah-specific fields so the required topic ledger and evidence could be covered.
- Alignment friction: the bundle reports `word_analysis` has 11 orthographic units while QAC has 9 morphological words, with direct word-level joins marked unreliable and dangling upstream refs for `42:35:10` and `42:35:11`. I used `word_morpheme_spans` and QAC morpheme refs for Arabic surfaces and morphology rather than silently correcting upstream identities.
- Grounding friction: HFT later-context deltas and outliers provide refs, roots, branch IDs, roles, and changed readings, but often not full Arabic surfaces for later-ayah words. I cited later context by reference and grounded the prose in this ayah's own words instead of inventing later Arabic spans.
- Channel scope friction: one channel subchannel is anchored at 42:35, but the bundle labels channel review as first-pass discovery rather than an adjudicated channel ledger or maturity plan. I used it only as local candidate support where already grounded in `مَّحِيصٍۢ`, and did not name or assert a surah-wide channel.
- Coverage limitation: `v12_reader_responses` are absent under the retired per-ayah workflow, and no whole-surah reading exists for S42. HFT, regular reader walk, wide reader walk, cross-run publication, inter-ayah rows, root lexicon, and branch inventories are present and used within their stated limits.
- Style audit: the prose necessarily carries the ayah's own negative proposition. Explicit correction was kept for live risks: direct-object versus domain for the signs, local lexical sense versus secondary branch resonance, and no-exit versus mercy/repair. This pushed the prose near the profile's negation ceiling, but the negatives are tied to the ayah's own grammar and containment needs rather than rhetorical contrast.
