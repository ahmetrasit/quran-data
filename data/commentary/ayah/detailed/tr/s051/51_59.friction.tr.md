=== PROMPT FRICTION ===

Run went cleanly overall. I found no direct contradiction that prevented completion.

Two constraints needed careful handling:

- The prompt says to include candidates with distinct payoff, while also increasing depth through precision rather than bulk. I handled this by grouping overlapping candidate payoffs into phrase-level integrations instead of giving each topic its own prose unit.
- Channel material is present but explicitly first-pass and non-adjudicated. I used it only to shape the local bucket/share and ordered-turn reading, and marked that use in the evidence surface as channel-informed writer integration.

Missing-source handling:

- `v12_reader_responses` and `v12_focus_trace_hermetic` are empty, so I did not infer staged isolated readings, confidence movement, baseline models, context deltas, or surprising outliers.
- The bundle reports no whole-surah reading file; I treated the regular reader walk, plus/minus-5 reader walk, and cross-run publication as coverage/priority checks.

Negation check:

- Explicit negative predicates in prose were used only where the reader could plausibly replace the local sense: the `dhanūban` sentence protects "measured share" from being flattened into "bare sin," and the final warning preserves the live "cannot hasten" force. I kept them under the profile ceiling and avoided stacked negatives.
