=== PROMPT FRICTION ===

Run completed cleanly overall.

Friction noted:

- The bundle explicitly reports `word_analysis_qac_alignment.consistent=false` because word_analysis counts nine analytic words while QAC counts six morphological words. I resolved this by using `word_morpheme_spans` for surface/QAC joins and retaining word_analysis topic ids only as topic ids.
- The prompt asks for no wrapper labels in prose/evidence files, while the friction output must have the exact heading `=== PROMPT FRICTION ===`; I applied the heading only in this friction file.
- `v12_reader_responses` and `v12_focus_trace_hermetic` are absent, so before/after material had to be limited to regular reader-walk and wide reader-walk retrospective observations. No staged reveal language was used.
- The prose keeps explicit negative predicates within the default ceiling and uses them only where a likely replacement risk needed containment: negation scope over creation, divine-need/transactional reading, and objectless worship-state limitation.
