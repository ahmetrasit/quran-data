=== PROMPT FRICTION ===

- Output contract conflict: “Your response” asks for three parts and omits the findings index, while the task document, `COMMENTARY_SPEC.md`, and the orchestrator request require a separate index. The explicit orchestrator request and the fuller governing contract were followed, so four files were written.
- Grounding conflict: three candidate topics depend on 50:16 or 10:57, but the compact bundle gives only their references and semantic roles. The task simultaneously requires the later ayah’s word to receive its own full Arabic span. `114:5:2:rare-field-role-contrast`, `114:5:4:chest-echoes-and-role-boundary`, and `114:5:5:human-interiority-and-whisper-field` were therefore excluded or covered only through independent local topics; no Arabic surface was reconstructed from outside the bundle.
- Root identity conflict: the scoped branch inventory associates QAC `ن و س` with `root_004965` and swaying/driving branches, whereas `root_lexicon` maps the same QAC root to Furuq `root_000059` / `ء ن س` and gives people, perception, and familiarity branches. The word analysis follows the latter path while the reader walks and cross-run findings follow the former. Both pressures were preserved without claiming that the two root identities are equivalent, and the swaying synthesis was marked as inference.
- Style audit, not friction: the prose uses one explicit negative predicate, “Fiilin açık bir nesnesi yoktur,” because the objectless frame is a live grammatical finding. No other misconception, replacement risk, or counter-evidence required an explicit negative predicate; the result stays below the profile ceiling of three.
