=== PROMPT FRICTION ===

1. Promptun “Your response” bölümü üç parça isterken ayet düzeyi sözleşmesi ayrıca findings index ister. Orchestrator dört ayrı dosyayı açıkça zorunlu kıldığı için prose, evidence, index ve friction olarak dört artifact üretildi.

2. `word_analysis` 26 analitik birim kullanıyor, QAC ise 20 ortografik kelime kullanıyor. `aligned_qac_word_ref` alanının son altı değeri dangling ve coverage bunu upstream defect olarak doğruluyor. Kimlik uydurmamak için topic ID'leri aynen korundu, morfoloji atıflarında yalnız güvenilir `word_morpheme_spans[].qac_refs` kullanıldı.

3. Hermetic Focus Trace `packet_summary.split_root_mappings: []` ve coverage `split_root_count: 0` bildiriyor; aynı trace `root_001650` ve `root_000743` non-dominant eşlemelerini etkin biçimde kullanıyor, `root_lexicon` da `س م و` için bir dominant ve iki non-dominant split target kaydediyor. Yerel sürprizlerde ayrıntılı `root_lexicon.root_mapping_role` kaydı esas alındı ve çelişki evidence yüzeyinde korundu.

4. Profil, anchored kalan surprising outlier'ların korunmasını isterken grounding kuralı, başka ayetin açtığı okumada o ayetin kelimesinin tam Arapça yüzeyini zorunlu kılıyor. `outlier_promise_as_irregular_calendar` ile `outlier_rain_as_withheld_rescue` 45:32 ve 45:34 tarafından açılıyor; bundle bu tetikleyici kelimelerin kök ve word-index kayıtlarını veriyor, tam yüzeylerini vermiyor. Dış kaynak kullanma yasağı nedeniyle iki outlier prose/indexe alınmadı ve evidence'da açık exclusion olarak bırakıldı.

5. Üç ayrı `must_integrate` konu 45:3-5 alıcı kapanışlarındaki iman, yakîn ve akletme değişimini zorunlu kılıyor; bundle bu anlamları ve ayet referanslarını veriyor, 45:3 ve 45:4 kelimelerinin tam Arapça yüzeylerini vermiyor. Zorunlu payoff Türkçe ve ayet referanslarıyla prose'a alındı; başka ayet kelimesi için structured span şartı aynı anda tam karşılanamadı. Bu, bundle ile grounding sözleşmesi arasındaki giderilemez eksikliktir.

6. “Distinct reader payoff” için mekanik bir eşik tanımlanmamış. Candidate kararında, aynı sonucu zaten taşıyan must/surprise satırları tekrar sayılmadı; kabul ve exclusion listeleri evidence'da tek tek kaydedildi.

7. Profilin “context_deltas kullan” talebi kullanımın prose'a zorunlu giriş mi, coverage/karşı-kanıt denetimi mi olduğunu belirtmiyor. On dört delta incelendi; ayetin kendi sözleriyle yerel olarak kurulabilen audience-mode, circulation ve reason-binding etkileri kullanıldı. Tam later-ayah yüzeyi isteyen uzak deltas prose'a taşınmadı.

**Stil denetimi:** Prose'da bir açık olumsuz yüklem bırakıldı: uzak split-root benzetmelerinin gök ve gündüzün yerel anlamını değiştirmediği cümlesi. Bu yüklem canlı replacement riskini düzeltmek için gerekliydi ve profil tavanı olan üçün altında kaldı. Diğer karşıtlıklar olumlu yüklemlerle yeniden kuruldu.
