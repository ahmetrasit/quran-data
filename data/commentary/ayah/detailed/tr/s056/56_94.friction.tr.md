=== PROMPT FRICTION ===

Run mostly clean.

The main data friction was the bundle's own `word_analysis_qac_alignment.consistent = false`: word-analysis treats `وَ`, `تَصْلِيَةُ`, and `جَحِيمٍ` as three analytic words, while QAC has two word units because `وَ` is a prefix on the first word. I handled this by citing QAC morpheme refs for morphology and word-analysis topic IDs for interpretive claims, without renumbering anything.

The channel instructions were clear enough but required restraint: one first-pass channel anchor exists, while no adjudicated ledger or maturity exists. I used it only as an explicitly marked local inference about hostile reception language in the evidence surface and did not name an established surah channel in prose.

I used two explicit negative predicates in the prose: one to prevent the opening `وَ` from being heard as causal explanation, and one to preserve the rejected/boundary status of the "following the blaze" reading. The likely Turkish misconception around `salât`/`namaz` was handled with positive predication rather than an additional explicit negation.
