=== PROMPT FRICTION ===

Run mostly clean. I found three friction points:

1. The bundle says `v12_reader_responses` are absent/retired, while the task document still says to say so if both staged reader responses and Hermetic Focus Trace are absent. I kept that absence in the evidence coverage note, not in reader prose, following the later profile and the "absence goes in coverage note" rule.

2. `word_analysis` counts four critical words and gives `aligned_qac_word_ref` 56:84:4 for `تَنظُرُونَ`, while QAC has three word refs and the coverage block marks 56:84:4 as dangling. I used `word_morpheme_spans` and the preserved word-analysis topic ids instead of silently renumbering.

3. The prompt requires positive predication but also permits explicit negatives for misconception/replacement/counter-evidence. I used three explicit negative predicates in prose: one to keep `تَنظُرُونَ` from absorbing causative/respite agency, one quoted later-context contrast ("siz görmezken"), and one final synthesis boundary ("bakışına geçmez"). I avoided stacking two negatives in one sentence.
