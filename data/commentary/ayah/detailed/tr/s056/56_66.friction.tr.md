=== PROMPT FRICTION ===

Run mostly clean. I found no contradiction that prevented completion.

Two points required judgment:

- `word_analysis` uses three word refs and refers to `56:66:3`, while QAC has two word refs because `لَ` and `مُغْرَمُونَ` are morphemes inside one QAC word. I followed the bundle coverage note and used `word_morpheme_spans` for joins instead of renumbering.
- The profile says to include every candidate with a distinct payoff, while also asking for increased depth without extra bulk. I integrated the candidates in connected prose where they added a live payoff, and compressed overlapping candidate material in the evidence surface.

Explicit negative predicates in the prose were kept to two live correction points: the loss is more than the missing crop, and the local B004 attachment branch does not replace the burden sense.
