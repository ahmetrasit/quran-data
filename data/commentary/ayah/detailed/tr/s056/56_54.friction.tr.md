=== PROMPT FRICTION ===

Run completed cleanly overall.

The main underspecified point was the exact schema for the evidence surface. I used an addressable phrase map with refs, claim type, and a coverage note because the prompt requires addressability and inference marking but does not prescribe a machine-readable format.

`v12_focus_trace_hermetic` appears as an empty object in the JSON while the rendering profile says “if present” use its fields. I treated it as absent because it contains no `baseline_models`, `context_deltas`, or `surprising_valid_outliers`.

The prompt asks to write exactly three files, while the destination directory already contains many other ayah files. I interpreted this as “edit only the three assigned output files” and did not alter existing files.

I used explicit negative predicates in the prose only where they protected likely misconceptions or preserved live ambiguity: that `شَٰرِبُونَ` is not a finite verb, that the backward pointer in `عَلَيْهِ` should not be resolved to one antecedent, and that drinking is not inherently punitive. I kept within the default ceiling of three.
