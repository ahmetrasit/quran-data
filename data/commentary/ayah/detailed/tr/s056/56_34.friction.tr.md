=== PROMPT FRICTION ===

The run was mostly clean.

One friction point: the bundle’s word_analysis uses `56:34:3` for `مَّرْفُوعَةٍ`, while qac_morphemes has only QAC words `56:34:1` and `56:34:2`; the coverage note flags this as an upstream defect caused by word_analysis counting the prefixed `وَ` separately. I followed the prompt’s instruction not to repair identities and used word_morpheme_spans/qac_morphemes for exact morphology.

Second friction point: the prompt says to include every candidate with a distinct payoff, but this short ayah has many candidate topics whose payoff overlaps once integrated into connected prose. I carried the distinct ones in prose and marked two tightly overlapping form topics, the sukun variant and possible forward antecedent, as bounded coverage in the evidence surface instead of adding extra prose sentences that would over-weight them.

Third friction point: channel material exists only as first-pass single-reader review, while reader-walk material also invites later contextual sharpening. I used channel material only as local word-connection pressure and used reader-walk/cross-run material only for retrospective sharpening, not as an established channel or surah thesis.

Prose negation check: I used one explicit negative predicate in the prose, to protect `فُرُشٍۢ` from being flattened into one abstract furnishing mass. Other contrasts were written as positive containment.
