=== PROMPT FRICTION ===

Run went cleanly overall. I found two manageable frictions:

- The prompt requires a section headed exactly `=== PROMPT FRICTION ===`; because this run writes a dedicated friction file, I kept that heading inside the file.
- Word-analysis refers to three analytic words while QAC has two word indices. The bundle explicitly flags this and provides `word_morpheme_spans`, so I used those spans rather than repairing or renumbering identities.

I used three explicit negative predicates in the prose to protect likely misconception/replacement risks: `عِينٌۭ` as a second coordinated provision, `عِينٌۭ` as counted eyes, and the helper-disciple branch as the local name. No further explicit correction was needed.
