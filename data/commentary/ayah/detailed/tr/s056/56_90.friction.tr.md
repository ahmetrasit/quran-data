=== PROMPT FRICTION ===

Run followed the prompt without needing external files, scripts, helpers, or delegated agents.

Main friction: the general layer-2 instruction says exclusions and branch inventories are handed forward, while the 5.6 profile says branch inventories support explanations and create no standalone prose obligation. I resolved this by rendering every word-analysis `must_integrate` topic and every candidate with distinct reader payoff, while using the scoped branch inventory only where it clarified those topics or reader-walk findings.

Second friction: `word_analysis_qac_alignment` is explicitly inconsistent for this ayah, because QAC counts 6 words and word-analysis counts 7 orthographic/analytic words. I used `word_morpheme_spans` for joins and cited both local morpheme refs and topic IDs in evidence.

The prose uses two explicit negative predicates, both for likely replacement risks: sentence completion before 56:91 and `مِنْ` as a loose appendage or spatial departure. The `كَانَ` risk was handled by positive predication instead.
