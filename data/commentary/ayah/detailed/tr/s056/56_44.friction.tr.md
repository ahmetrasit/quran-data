=== PROMPT FRICTION ===

Run went cleanly. I did not need to invent a rule beyond the prompt's own coverage instruction.

The main operational friction was the bundle's acknowledged `word_analysis_qac_alignment` mismatch: word-analysis has five orthographic units while QAC has four word refs. I followed the bundle note and used `word_morpheme_spans` for joins.

The explicit-negation ceiling was workable but tight because the ayah itself is a paired negation. I preserved the primary "ne ... ne ..." force and rewrote most explanatory contrasts as positive predication.
