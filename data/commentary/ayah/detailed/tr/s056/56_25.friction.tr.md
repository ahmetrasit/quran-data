=== PROMPT FRICTION ===

Run mostly cleanly, with three specification/data frictions.

1. The prompt asks for evidence surfaces to hold refs and branch IDs, while also warning that word-analysis/QAC joins are unreliable here. I used word-analysis topic IDs for commentary obligations and QAC morpheme refs only through the supplied `word_morpheme_spans`, especially for `تَأْثِيمًا`, where word-analysis `56:25:7` does not resolve as a QAC word.

2. The prompt permits reading external `channel_generated_outputs` files if necessary, but the user instructed work only from the assigned prompt and to edit only the three outputs. I treated the inlined channel manifest and anchored subchannels as sufficient, did not read external channel files, and avoided established-channel claims.

3. The negation rule asks for positive predication with a default ceiling of three explicit negative predicates. This ayah is itself a double negation, so the prose necessarily uses reader-facing “ne... ne de” language. I kept one explicit corrective negative for `لَغْوًۭا` as heard object rather than residents' speech; the non-prohibitive `لَا` correction is phrased through “emir tonu yerine.” The remaining denials are rendered as exclusion, protection, clearing, and removal.
