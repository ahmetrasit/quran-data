=== PROMPT FRICTION ===

The run was mostly clean. One practical ambiguity remained: the prompt asks for an “evidence surface” but does not prescribe a schema, so I used an addressable phrase table with explicit `Bundle-traceable` versus `Inference` status.

The bundle records unreliable word-analysis-to-QAC word alignment for this ayah. I resolved that by using `word_morpheme_spans` for joins and by citing QAC morphemes directly where grammar mattered.

The channel material is first-pass only, while the prompt also allows channel-generated files to be read when necessary. I did not read external channel files because the inlined anchored review and reader walks already supplied enough local evidence, and reading more would have increased the risk of implying an adjudicated channel.

I used three explicit negative predicates in the prose, within the profile ceiling: “kapanmaz” to preserve the hapax openness, “yerini almaz” to protect the primary botanical sense from the rejected distant weariness-to-support metaphor, and one “değil” to clarify that the canonical wording gets its layering from the adjective rather than from a more direct palm-cluster noun.
