=== PROMPT FRICTION ===

Run mostly clean.

Potential ambiguity: the prompt says to include every candidate with a reader payoff not already expressed, but does not define how granular “already expressed” must be. I treated candidates as covered when their distinct payoff had a clear sentence-level home in the integrated prose, rather than giving every candidate a separate prose sentence.

The bundle contains `channel_subchannels_anchored_here` and `channel_generated_outputs`, but no adjudicated channel ledger. I used the channel material only as a local connection check and marked the relevant synthesis in evidence as inference/channel-informed; I did not name or assert a surah channel in the prose.

`v12_reader_responses` and `v12_focus_trace_hermetic` were empty while reader walks and cross-run publication were present. I used the reader walks for retrospective insight and avoided staged-reveal language.

The prose uses one explicit negative predicate to prevent a likely grammatical misreading: the `مَسَّ` paragraph says the form does not open a new finite clause. Other contrastive obligations were rendered with positive containment.
