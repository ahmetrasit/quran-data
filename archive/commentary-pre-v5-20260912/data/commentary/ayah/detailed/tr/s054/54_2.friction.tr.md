=== PROMPT FRICTION ===

Run clean. I found no impossible instruction and did not invent a rule to proceed.

The only point requiring care was the bundle's own warning that word_analysis word numbering and QAC word numbering diverge for this ayah. I followed that warning by using word_analysis topic ids and the supplied attachment/QAC references without trying to renumber them.

I used explicit negative predicates only where they protected a live replacement risk: chiefly that `مستمر` modifies the quoted magic-label rather than the earlier sign. Other contrasts were rendered with positive predication.
