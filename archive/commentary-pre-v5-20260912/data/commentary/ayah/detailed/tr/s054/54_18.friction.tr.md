=== PROMPT FRICTION ===

Run mostly clean. The main friction was the coexistence of a reader-walk return-echo for `عَادٌ` with a cross-run-publication item that grades a similar formulation as `reject`. I resolved this by keeping the return material only as bounded pressure/inference from the reader walks and word-analysis candidate, while noting the cross-run rejection in the evidence surface.

The word-analysis/QAC word alignment is explicitly inconsistent for this ayah because the analysis counts eight orthographic surfaces while QAC counts six morphological words. I used `word_morpheme_spans` for references and reported the alignment issue in coverage.

No channel increment was rendered: the bundle has only first-pass single-reader channel review, zero subchannels anchored here, and no adjudicated ledger. Per-ayah v12 reader responses and Hermetic Focus Trace were absent, so I did not infer staged isolation or confidence movement. Whole-surah reading was also absent.

Explicit negative predicates in prose were revised down into positive predication. The remaining contrastive language protects the primary sense through "as/while" containment rather than overt correction, so there was no unresolved negation-policy conflict.
