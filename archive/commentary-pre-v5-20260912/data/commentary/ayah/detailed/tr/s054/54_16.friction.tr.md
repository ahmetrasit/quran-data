=== PROMPT FRICTION ===

Run mostly clean.

One ambiguity: the prompt says to include every candidate with a distinct payoff, while channel material includes a latent State B subchannel anchored partly in `عَذَابِى` through a nonlocal gestational/emergence family. I treated that channel as non-authoritative first-pass material and did not name or develop it in prose, because the same prompt forbids asserting an established channel under State B and the subchannel itself is marked `reading_type: latent`.

Another ambiguity: `v12_focus_trace_hermetic` appears as an empty object in the bundle body and later as `present: false` in coverage. I followed the coverage block: no Hermetic before/after trace was rendered. Regular reader walks, wide reader walks, and cross-run publication findings were used instead.

Explicit negative predicates were kept under the requested ceiling and used only to protect one likely replacement risk: vow/adak pressure becoming the local meaning of `نُذُرِ`.
