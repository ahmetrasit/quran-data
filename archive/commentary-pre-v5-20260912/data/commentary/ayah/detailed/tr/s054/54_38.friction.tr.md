=== PROMPT FRICTION ===

Word-analysis and QAC word identities diverge for this ayah: the bundle has 7 analytic word-analysis units but only 5 QAC word refs, and coverage explicitly marks word-level joins as unreliable. I proceeded by using `word_morpheme_spans` plus topic ids for evidence mapping, and did not repair or renumber identities.

The bundle includes named channel subchannels anchored here, while the governing rules forbid rendering first-pass channel material as an established channel. I treated those records as local connective evidence only, especially for dawn separation and settlement language, and avoided channel names in the prose.

The prompt says to say when both staged reader responses and Hermetic Focus Trace are absent, but also says absences belong in the coverage note, never in prose. I followed the latter for reader-facing prose and reported the absence in the evidence coverage note.

The prose uses one explicit correction because the likely misconception is live in the bundle: `مُّسْتَقِرٌّ` as a new finite action rather than the final adjective of `عَذَابٌ`. I rewrote the other likely corrections as positive predication and did not stack negative predicates.
