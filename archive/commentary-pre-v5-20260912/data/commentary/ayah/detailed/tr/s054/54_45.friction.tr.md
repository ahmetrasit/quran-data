=== PROMPT FRICTION ===

Run mostly clean.

The prompt asks for three response parts but the user requested three separate files. I followed the file request and omitted wrapper labels from the prose file; the evidence file uses only an internal coverage heading because coverage is part of the evidence surface.

State B channel material was present through `channel_subchannels_anchored_here`, while `channel_generated_outputs` also listed external files. I treated the inlined channel material as first-pass local evidence only, did not name an established channel, and did not read external channel files because no additional channel-family/path detail was necessary.

There was a live risk that the `د ب ر` management/reflection pressure could become a standalone strategic wordplay; `v12_cross_run_publication` explicitly rejects that as an independent finding. I therefore kept it as secondary pressure under the concrete final "back" image.

Explicit negative predicates were kept below the requested ceiling and used only where they protected the primary surface from replacement or clarified a likely misconception.
