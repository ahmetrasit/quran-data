=== PROMPT FRICTION ===

1. The task document says that skipping the reader walk is a rejection-level failure, while this bundle reports both regular and wide reader walks as unavailable because zero ayah headings were recognized. The rendering profile says to use such material only when present and distinct. I followed the actual coverage state, used the cross-run publication only as a compact check, and reported the missing walks in the evidence surface.
2. The bundle contains a material identity conflict: word analysis uses 16 analytic/orthographic units, QAC uses 12 words, 12 `word_morpheme_spans` are unresolved, and later `aligned_qac_word_ref` values dangle. I did not repair or infer the crosswalk. The evidence surface keeps word-analysis topic references and independently matching QAC morpheme references separate.
3. “Include every candidate with a reader payoff not already expressed” requires a judgment about when overlapping payoffs are already expressed. I treated candidates as mergeable only when one sentence delivered their shared payoff, while retaining distinct variant, sound, form, root-pressure, and inter-ayah payoffs.
4. No additional rule had to be invented.
