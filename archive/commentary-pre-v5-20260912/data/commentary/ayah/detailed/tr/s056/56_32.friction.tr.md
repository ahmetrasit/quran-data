=== PROMPT FRICTION ===

The run was mostly clean.

One friction point: the bundle’s word_analysis uses `56:32:3` for `كَثِيرَةٍۢ`, while qac_morphemes has only QAC words `56:32:1` and `56:32:2`; the coverage note flags this as an upstream defect. I followed the prompt’s instruction not to repair identities and used word_morpheme_spans/qac_morphemes for exact joins.

Second friction point: the prompt requires carrying reader-walk material, but the channel material is first-pass and not a ledger. I used the orchard/clustering evidence only as local, word-anchored interpretive pressure and marked the integration as inference in the evidence surface.

Prose negation check: I used explicit negative predicates only where they protected the primary reading from likely replacement risks: the ayah is not a detached assertion, and the later abundance is not an autonomous stock. Other contrasts were rewritten as positive predication.
