=== PROMPT FRICTION ===

Run went cleanly overall. I did not need to invent a rule to proceed.

Two prompt tensions required explicit handling:

- The bundle permits reading listed external `channel_generated_outputs` files when necessary, while the top prompt says the file is self-contained by default and the user asked to work only on this ayah/prompt. I treated the inlined channel subchannel as sufficient and did not read external channel files.
- The prompt asks to include every candidate with distinct payoff while also increasing depth through precision rather than bulk. I compressed overlapping sound, boundary, and later-context payoffs into integrated prose rather than giving each candidate its own paragraph.

Source gaps were handled in the evidence file, not the prose: retired per-ayah `v12_reader_responses`, absent `v12_focus_trace_hermetic`, and absent whole-surah reading.

Likely replacement risks existed: reading `نُزُلٌۭ` as an actual descent verb, treating `حَمِيمٍۢ` as ordinary warmth, and making `حَمِيمٍۢ` an independent second verdict rather than the governed complement. I handled them through positive containment in the prose, so no explicit negative predicate was needed.
