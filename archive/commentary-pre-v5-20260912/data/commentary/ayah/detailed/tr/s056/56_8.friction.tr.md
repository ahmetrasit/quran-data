=== PROMPT FRICTION ===

Run completed cleanly. I found no blocking contradiction or underspecified requirement that forced a new rule.

Two implementation notes:

- The bundle notes a word-analysis/QAC alignment mismatch for this ayah. I used the `word_morpheme_spans` crosswalk and the `critical_w` word-analysis order rather than joining by apparent word number.
- The prompt allowed reading listed `channel_generated_outputs` only if channel-family or path detail was necessary. I did not read those external files because the prose did not need a channel-family/path claim, and State B restrictions prohibit rendering an established channel anyway.

Explicit negative predicates were kept below the requested ceiling and used only to protect likely misconceptions: ordinary information-question reading for `مَآ`, ordinary social-company overread for `أَصْحَٰبُ`, and location-only flattening for `ٱلْمَيْمَنَةِ`.
