=== PROMPT FRICTION ===

Run completed cleanly. I found no contradiction that prevented following the prompt.

Implementation notes:

- The bundle contains an early empty `{}` form for `v12_focus_trace_hermetic` and later normalized metadata marking it `present: false`; I treated Hermetic Focus Trace as absent and did not infer staged or reconstructed before/after evidence.
- `v12_reader_responses` are explicitly retired/absent in this workflow, so I used regular reader walks, wide reader walks, and cross-run publication findings only as coverage checks.
- The channel material is first-pass single-reader review, not an adjudicated ledger. I used it only to inform an ayah-word-anchored connection in the evidence surface and avoided naming an established channel.
- I used two explicit negative predicates in the prose to protect likely replacement risks: `hasarı değil` for `حُطَامًا` as result complement, and `gibi değil` for `ظَلْتُمْ` under counterfactual force.
