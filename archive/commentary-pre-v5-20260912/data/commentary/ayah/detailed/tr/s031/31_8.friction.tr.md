=== PROMPT FRICTION ===

- “Your response” bölümü üç parça isterken görev belgesi ve orchestrator dört ayrı artifact, buna findings index dâhil, istiyor. Dosya sahipliği açık ve daha özgül olduğu için dört dosya yazıldı; prose ve evidence dosyalarına wrapper etiketi eklenmedi.
- Grounding kuralı, sonraki âyetin bir kelimesine dayanıldığında o kelimenin tam `{ar, tr, gloss}` spanını ister. Gömülü Hermetic Focus Trace ise context delta satırlarında yalnız kök, kelime konumu ve rol verir. Bundle coverage alanında exact `packet_source_file` yolu manifest edildiği için yalnız bu dosyadaki `text_ar` ve `root_occurrences` alanları yüzey doğrulaması amacıyla okundu. Genel “manifestte adı geçen exact dosya okunabilir” kuralı bunu destekliyor; profile’ın dış dosya iznini yalnız `channel_generated_outputs` için açıkça yinelemesi izin kapsamını yine de belirsiz bırakıyor.
- Word-analysis 10 ortografik/analitik birim sayarken QAC 8 kelime sayıyor ve ham `aligned_qac_word_ref` alanlarında iki dangling kimlik bulunuyor. Bundle bu refs alanını düzeltmeyi yasaklıyor ama topic kimlikleri aynı 10’lu sayımı kullanıyor. Topic refs indexte aynen korundu; morfoloji iddialarında `word_morpheme_spans` kullanıldı ve yeni QAC ref icat edilmedi.
- “Her ayrı payoff’u olan candidate’ı al” kuralı ile “surprise veya branch sayısı tek başına kabul gerekçesi değildir” kuralı bazı birbirine yakın ses, formül ve yapı adaylarında kesin bir eşik tanımlamıyor. Bu çalışmada her candidate’ın payoff’u bütünleştirilmiş prose içinde ayrı bir okur sonucu olarak kurulabildiği için 41 candidate’ın tamamı alındı; karar evidence dosyasında açıklandı.

**Profile-specific style audit:** Prose’da üç açık olumsuz yüklem/istem vardır ve hiçbiri aynı cümlede yığılmamıştır: `demek değildir` yerel `sâlihât` biçimini kök onarım baskısının yerine geçmekten korur; `itaat etme` 31:15’in bundle’daki canlı ilişki sınırıdır; `üstlenemez` 31:33’ün ücret benzetmesini sınırlayan karşı kanıtıdır. Tavan üçtür; diğer karşıtlıklar olumlu yüklemle yazılmıştır.
