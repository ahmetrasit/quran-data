- The bundle is internally inconsistent at word identity level: `word_analysis` has 56 orthographic units, QAC has 44 words, and `word_morpheme_spans` resolves only four units. I did not renumber or repair identities; evidence cites topic IDs and records the defect in coverage.
- The prompt says to read the full self-contained prompt, but the embedded JSON is too large to inspect usefully as raw terminal output. I read the governing prompt sections and queried the structured bundle fields directly from the named bundle file to avoid raw-line truncation while preserving the same evidence source.
- The boundary between `candidate` word-analysis topics and mandatory "what each word contributes" remains underspecified. I integrated all `must_integrate` topics, admitted only candidates needed for coherent local synthesis, and did not turn all 119 candidate topics into prose.
- The focus-trace context deltas include later ayah activations whose exact Arabic word surfaces are not always exposed in the compact prose-ready fields. To avoid ungrounded disclosure, the prose renders those deltas through the 14:22 words already on the page and leaves detailed later-reference attribution in the evidence surface.
- The channel material is first-pass and explicitly not an adjudicated channel ledger. I used the anchored subchannel only as support for a local surprise about failed promise/call/rescue relations, without naming a surah-wide channel or claiming maturity.
- No blocker remained.
