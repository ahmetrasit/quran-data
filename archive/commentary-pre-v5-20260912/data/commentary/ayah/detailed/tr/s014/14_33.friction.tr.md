=== PROMPT FRICTION ===

- Prompt ayah-level output contract includes a findings index as a separate artifact, but the user allowed writing exactly three files: prose, evidence, and friction. To satisfy both as closely as possible without creating a fourth file, I placed the findings index inside the evidence file under its own heading.
- The bundle coverage marks `word_analysis_qac_alignment.consistent: false`; word-analysis topic refs count orthographic units while QAC counts morphological words. I used `word_morpheme_spans` for surface/QAC grounding and kept the upstream topic ids unchanged in evidence and findings.
- `v12_reader_responses` is absent by design in this workflow, while `v12_focus_trace_hermetic` is present as reconstructed before/after evidence. I used HFT and reader walks for later-context changes and did not invent staged reader movement.
- Channel review is present but has zero subchannels anchored here, and generated channel outputs are candidate discovery material. I did not read external channel files or name a channel.
- Style audit for negation: explicit negative predicates were used only where the prose needed to protect local sense from likely replacement or overextension: ownership under `لَكُمُ`, local meaning under root disputes/branch echoes, and human agency under later context. I kept them sparse and avoided stacked negative correction.
