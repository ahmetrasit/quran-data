=== PROMPT FRICTION ===

The bundle contains an explicit identity mismatch: `word_analysis` numbers 13 orthographic/analytic units, while QAC numbers 11 morphological words; `18:43:12` and `18:43:13` therefore do not resolve as QAC word refs. The prompt correctly instructs against silent renumbering, but this makes a single word-level citation scheme impossible. I kept word-analysis topic IDs and QAC morpheme refs separate and used `word_morpheme_spans` for Arabic surfaces.

The candidate `18:43:5:fallback-return-image` describes a return/fallback coloring and says V4 has no guardrail rows, while the included scoped branch inventory for `ف ء ي` contains three branches and identifies the local branch as `B003`, “a separated group of people,” without a return branch. Because the rendering profile asks for every candidate with a distinct payoff, the prose retains the fallback pressure as a contained secondary coloring; the evidence surface does not assign it a V4 branch ID. This internal tension should be resolved upstream.

The task document requires the absence of both staged reader responses and Hermetic Focus Trace to be stated, while also requiring source absence to stay out of prose. I followed the later, more specific instruction and recorded both absences in the evidence coverage note.

No additional rule had to be invented. Three explicit negative predicates were retained because the ayah itself consists of two denials and the rain-branch sentence required a replacement guard; the remaining contrasts were rendered by positive predication.
