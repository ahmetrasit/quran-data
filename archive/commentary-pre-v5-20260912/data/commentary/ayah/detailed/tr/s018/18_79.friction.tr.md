=== PROMPT FRICTION ===

1. The task says that when staged reader responses and Hermetic Focus Trace are both absent, “say so,” while the structure rule says source absence belongs only in the coverage note and never in prose. I reported both absences in the evidence file’s coverage note.

2. The bundle says all 21 `word_morpheme_spans` resolve, but it also marks `word_analysis_qac_alignment` inconsistent because word analysis counts 21 analytic units and QAC counts 17 words; the preserved `aligned_qac_word_ref` values `18:79:18-21` therefore dangle. I kept topic IDs stable, used explicit QAC morpheme refs only where the supplied QAC record directly supports them, and invented no cross-layer renumbering.

3. The word-analysis surfaces and the Uthmani ayah surface differ in diacritics and orthographic presentation. I used the supplied Uthmani forms in structured prose spans and retained the analytic topic identities in evidence.

4. “Include every candidate with a distinct payoff” creates substantial density alongside the instruction to increase semantic precision rather than bulk. I integrated overlapping grammar, root-pressure, sound, recurrence, and contextual candidates into shared paragraphs, then mapped the topics separately in the evidence surface.

5. The first-pass channel review names two subchannels anchored here but supplies no adjudication or maturity. I used one local ownership-work-defect-seizure connection as explicitly marked inference and made no established channel or surah-thesis claim.

6. The root lexicon reports no mapping or dictionary payload for `ع ي ب`, while the scoped fallback inventory and reader walks preserve three branches for it. I cited those fallback branch identities without fabricating a root ID or dictionary entry.

7. The cross-run publication baseline token mapping does not align reliably with the QAC/analytic unit structure flagged elsewhere in the bundle. I used only its final anchored finding as a coverage check.

8. Two explicit negative predicates were retained in prose because they correct likely misconceptions: the poor are owners rather than mere beneficiaries, and `غَصْبًا` rules out neutral acquisition. Other contrasts were rendered positively.
