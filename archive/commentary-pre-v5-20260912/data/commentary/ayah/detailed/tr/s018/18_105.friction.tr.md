=== PROMPT FRICTION ===

The bundle contains an explicit identity mismatch: `word_analysis` and `word_morpheme_spans` count 19 orthographic/analytic units, while QAC counts 14 words and groups several prefixes, stems, and suffixes under shared word identities. Upstream analytic refs `18:105:15` through `18:105:19` therefore do not resolve as QAC word refs. The prompt forbids silent renumbering, so a single word-level citation scheme is impossible. I kept word-analysis topic IDs and QAC morpheme refs separate and used `word_morpheme_spans` as the Arabic surface authority.

The task document requires the absence of both staged reader responses and Hermetic Focus Trace to be stated, while also requiring source absence to appear only in the coverage note. I followed the more specific placement rule and reported both absences in the evidence coverage ledger rather than the prose.

The prompt requires each distinct candidate payoff to be included, but the candidate topics for `حَبِطَتْ` and `نُقِيمُ` refer to variant readings only through row IDs and do not inline the actual variant forms or their exact readings. Naming those forms would require invention. I therefore rendered only the bounded payoff that variant apparatus exposes nearby aspect, sound, or agency possibilities, while keeping the canonical QAC surfaces controlling; the limitation is explicit in the evidence surface.

The task says Layer 2 must pick up Layer 1 `consideredNotPrimary` readings, but the bundle contains no field or entries under that name. I used the available `status: narrowed` candidate topics and scoped branch inventory as the supplied non-primary pressures and did not invent missing Layer 1 selections.

The wide reader walk activates the swelling-to-zero image, while the compact cross-run publication marks that formulation `reject`. I treated this as live counter-evidence: the lexical swelling image remains contained inside the local deed-nullification sense, and the rejected causal formulation is identified only in the evidence surface.

Two explicit negative predicates were retained in the prose. One renders the ayah’s own categorical no-weight verdict; the other corrects the likely misconception that `وَزْنًۭا` names the scale instrument rather than the weight/value being assessed. Other contrasts were rewritten as positive predication.

No external channel file was needed. The first-pass channel review informed only the local connection among work, collapse, establishment, and weight; no established channel name, maturity, or surah thesis was asserted. No additional rule had to be invented.
