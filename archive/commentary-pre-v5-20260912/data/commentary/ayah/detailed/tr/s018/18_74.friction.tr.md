=== PROMPT FRICTION ===

The bundle contains an explicit identity mismatch: `word_analysis` and `word_morpheme_spans` count 20 orthographic/analytic units, while QAC counts 16 words and groups several prefixed particles, stems, and suffixes under shared word identities. Upstream analytic refs `18:74:17` through `18:74:20` therefore do not resolve as QAC word refs. The prompt forbids silent renumbering, so a single word-level citation scheme is impossible. I kept word-analysis topic IDs and QAC morpheme refs separate and used `word_morpheme_spans` as the Arabic surface authority.

The task document requires the absence of both staged reader responses and Hermetic Focus Trace to be stated, while also requiring source absence to appear only in the coverage note. I followed the more specific placement rule and reported both absences in the evidence coverage ledger rather than the prose.

The task says Layer 2 must pick up Layer 1 `consideredNotPrimary` readings, but the bundle contains no field or entries under that name. I used the available `status: narrowed` candidate topics and the scoped branch inventory as the supplied non-primary lexical pressures; I did not invent missing Layer 1 selections.

No external channel file was needed. The first-pass channel material was used only as corroboration for locally and retrospectively anchored connections, without naming an established channel or assigning maturity.

One explicit negative predicate was retained in the prose because later-context counter-evidence creates a live replacement risk: the later explanation does not invalidate Musa’s locally grounded protest. Other contrasts were rendered positively. No further rule had to be invented.
