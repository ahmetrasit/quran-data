=== PROMPT FRICTION ===

- Görev belgesi bulgular indexini ayrı ve zorunlu bir çıktı olarak tanımlarken `Your response` bölümü yalnız üç parça sayıyor. Orkestratör dört kesin dosya istediği için `prose`, `evidence`, `index` ve `friction` ayrı yazıldı.
- `word_analysis` 15 analitik birim, QAC 13 morfolojik kelime sayıyor; son iki `aligned_qac_word_ref` çözümsüz. Paket bu kusuru açıkça bildirip `word_morpheme_spans` kullanımını istediği için kimlikler düzeltilmeden morfem aralığı eşlemeleri esas alındı.
- Profil, temellenmiş Hermetic outlier ve bağlam değişimlerini korumayı isterken grounding kuralı başka âyetin sözcüğünü tam structured span olmadan kullanmayı yasaklıyor. Bağlam kayıtlarının çoğu yalnız kök, dal ve İngilizce rol veriyor. Bu yüzden yalnız odak âyete dayanan `outlier_imposed_mark` ile tam 24:24 Arapça çapası bulunan `ctx_body_counterwitness` yorumda işlendi; kalanlar kanıt kapsam notunda tutuldu.
- Genel kural `candidate` konuları okur getirisine göre ihtiyarî bırakıyor; profil ise daha önce ifade edilmemiş getiri taşıyan her `candidate`ı istiyor. "Daha önce ifade edilmiş" için biçimsel bir eşdeğerlik ölçütü verilmemişti. Aynı okur sonucunu yineleyen konular birleştirildi, tam komşu-sözcük zemini gerektirenler kapsam notuna alındı ve kabul/ret listesi kanıt dosyasında açıklandı.
- Structured span için "Turkish-readable transliteration" isteniyor fakat tek bir transliterasyon standardı belirtilmiyor. Türkçe okunuşa yakın `inne`, `ellezîne`, `yermûne`, `luinû` gibi biçimler kullanıldı; hemze ve uzunluk işaretleri yalnız okuru zorlamadan ayrımı korudukları yerlerde gösterildi.
- Ayrı friction dosyasında tam `=== PROMPT FRICTION ===` başlığının korunup korunmayacağı açık söylenmiyor. Zorunlu tam başlık talebi daha dar kural kabul edilerek başlık dosyada tutuldu.

Style audit: Yorum bir açık olumsuz yüklem (`Buradaki gaflet bir kusur değildir`) kullanır. Bu, Türkçedeki yaygın kusurlu "gaflet" çağrışımını düzeltmek için gerekliydi; üçlük tavanın altındadır ve aynı cümlede iki olumsuz yığılmamıştır.
