=== PROMPT FRICTION ===

- Görev belgesinin `Your response` bölümü üç parça ister ve bulgular indeksini saymaz; aynı belgenin bağlayıcı çıktı sözleşmesi ile orchestrator dört ayrı dosya ister. Dört dosyalı daha açık sözleşme izlendi ve indeks ayrı yazıldı.
- Word-analysis 31 ortografik birim, QAC 29 morfolojik kelime kullanıyor. Paket de hizalamayı `consistent: false` diye işaretliyor; `erûnî`, `fî` ve `i’tûnî` spanları çözümlenemiyor, son iki upstream kelime referansı sarkıyor. Kimlik uydurmamak için sorunlu noktalarda topic kimlikleri kullanıldı.
- Profil HFT’nin bağlam değişimlerini kullanmayı isterken grounding kuralı, sonraki âyetten yararlanılan her kelime için tam Arapça surface span ister. HFT birçok sonraki kelime için yalnızca âyet referansı, kök ve kelime indeksi veriyor. Pakette yüzeyi ayrıca görünen `تنزيل الكتاب`, `الكتاب` ve `لا يستجيب له` doğrudan kullanıldı; kalan değişimler odak âyetin kendi kelimelerine giren ve kanıt dosyasında `[inference]` diye işaretlenen baskılar hâlinde render edildi.
- `delta_appearance_and_residue` 46:24’ü görme-yorum farkı için etkinleştirirken inter-ayah satırı aynı âyeti `no value` sayıyor. Bu canlı karşı kanıt korunarak 46:24 anlatıda doğrudan açıklayıcı yapılmadı.
- Bazı `candidate` konuların `status` değeri `used`, bazılarının `narrowed`; bağlayıcı seçim ölçüsü `commentary_obligation` olduğu için durum etiketi karar kapısı olarak kullanılmadı. Ayrı okuyucu getirisi bulunan 13 adayın tamamı kabul edildi.
- Kanal malzemesi tek okuyuculu ilk geçiş ve olgunluk kararı içermiyor. Yerel sürprizi destekleyen gömülü dal kanıtları kullanıldı; kanal adı, tekrar veya sûre mimarisi ileri sürülmedi ve dış manifest dosyaları okunmadı.

Stil denetimi: Anlatıda tek açık olumsuz yüklem, 46:5’in pakette yüzeyi bulunan “ona karşılık vermez” ifadesidir. Bu olumsuzluk canlı karşı olayı korur; başka `-maz/-mez`, `değildir` veya `yoktur` yüklemi kullanılmadı ve olumsuzluklar aynı cümlede yığılmadı.
