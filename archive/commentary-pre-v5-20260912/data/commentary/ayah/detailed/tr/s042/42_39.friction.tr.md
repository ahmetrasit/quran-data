=== PROMPT FRICTION ===

- Output contract friction: the prompt asks for prose, evidence surface, findings index, and friction, while the user explicitly allowed writing only three paths and named no separate findings-index file. I placed the findings index inside the evidence file after the coverage note, following the existing S42 output convention and respecting the three-file write constraint.
- Prompt/source friction: the prompt says it is self-contained and not to read other files; the embedded bundle is a very large single-line JSON block that can be truncated by terminal display. I read the full task/spec sections from the prompt and queried the embedded JSON line from that same prompt with `jq` for ayah-specific fields, without reading the bundle file on disk.
- Alignment friction: the bundle reports `word_analysis` has 7 orthographic/analytic units while QAC has 6 word units, with direct word-level joins marked unreliable and dangling upstream ref `42:39:7`. I used `word_morpheme_spans` and QAC morpheme refs for Arabic surfaces and morphology rather than silently correcting upstream identities.
- Grounding friction: HFT later-context deltas and inter-ayah rows provide refs, roots, branch IDs, roles, and changed readings, but not full Arabic surfaces for later-ayah words. I cited later context by reference and grounded the prose in this ayah's own words instead of inventing later Arabic spans.
- Channel scope friction: three channel subchannels are anchored at 42:39, but the bundle labels channel review as first-pass discovery rather than an adjudicated channel ledger or maturity plan. I used them only as local candidate support where already grounded in this ayah's wording, and did not name or assert a surah-wide channel in prose.
- Coverage limitation: `v12_reader_responses` are absent under the retired per-ayah workflow, and no whole-surah reading exists for S42. HFT, regular reader walk, wide reader walk, cross-run publication, inter-ayah rows, root lexicon, branch inventories, pericope data, and channel review anchors are present and used within their stated limits.
- Style audit: explicit negative predicates were used only where a live misconception or replacement risk existed: defense as a detached exception, isabet as moral correctness, defense as unlimited counter-domination, and the hydrological outlier as translation. The prose exceeds the profile's default ceiling of three negatives because the prompt itself requires protecting primary sense from replacement while carrying two surprising outliers; the negatives are local containment safeguards rather than rhetorical contrast.
