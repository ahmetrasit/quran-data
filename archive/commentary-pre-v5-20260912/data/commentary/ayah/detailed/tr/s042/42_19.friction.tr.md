=== PROMPT FRICTION ===

- Output contract friction: the prompt requires a separate findings index, but the user explicitly allowed writing only three files and gave no findings-index path. I placed the findings index inside the evidence file after the coverage note, matching the existing `s042-default` pattern while respecting the three-file write constraint.
- Source-access friction: the prompt says the inlined bundle is self-contained, but the bundle is embedded as a very large single JSON block that the terminal output truncated. I used the explicitly named local bundle path `bundles/s042/42_19.ayah.json` read-only with `jq` to extract the same evidence. If strict hermetic operation means reading only the prompt file bytes, that procedural requirement is underspecified for large single-line bundles.
- Grounding friction: HFT later-context rows provide references, roots, branch IDs, cues, and inference summaries, but not full Arabic surface spans for the later ayah words. The prose therefore cites later ayahs by reference and Turkish concept labels rather than inventing Arabic spans from outside the bundle.
- Alignment friction: `coverage.word_analysis_qac_alignment.consistent=false`; word-analysis counts 11 orthographic/analytic words while QAC counts 9 morphological words, with dangling upstream refs for words 10 and 11. I used `word_morpheme_spans` for morphology joins and retained word-analysis topic IDs as their own identities.
- Channel scope friction: channel review material is present and two subchannels are anchored here, but the review is first-pass, non-adjudicated, and has no maturity ledger. I used it only as candidate local context for yield/rain readings and did not name a surah-wide channel.
- Coverage limitation: `v12_reader_responses` are absent because the default workflow retired per-ayah focus runs; whole-surah reading is absent. `ش ي ء` has no Turkish root dictionary/gloss entry, so its will/thingness pressure rests on word-analysis and branch inventory evidence rather than root_lexicon.
- Style audit: after revision, the prose uses no explicit negative predicate such as `değil`, `yoktur`, or `-mez/-maz` as a governing correction. I found no live misconception, replacement risk, or counter-evidence requiring a direct negative correction; containment is handled by positive predication and by marking analogy/inference in the evidence surface.
