=== PROMPT FRICTION ===

- Requested file set has no separate findings-index path, while the Layer 2 contract requires a findings index alongside prose, evidence, and friction. I placed the findings index inside the evidence file after the evidence surface so that only the three user-authorized files were written.
- The prompt requires later-ayah readings to be grounded with the later ayah's own word span. Several `v12_focus_trace_hermetic` context deltas cite later ayahs by root and role but do not inline the later Arabic surface forms. I rendered only the later-context material that could be grounded through this ayah's own words or through exact later formula text present in the trace, and I recorded the limitation in the coverage note.
- `word_analysis` critical word numbering diverges from QAC word numbering; the bundle itself flags this as an upstream alignment defect and provides `word_morpheme_spans`. I used topic IDs for obligations and QAC/morpheme span refs for morphology rather than silently renumbering.
- Style audit: one explicit negative predicate was kept because a live misconception existed around repentance as self-cleansing. Other contrastive sentences were rewritten as positive predication, keeping the prose within the profile ceiling and avoiding stacked negatives.
