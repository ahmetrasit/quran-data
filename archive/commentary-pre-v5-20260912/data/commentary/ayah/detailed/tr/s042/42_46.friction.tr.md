=== PROMPT FRICTION ===

- Output contract friction: the task/spec asks for prose, evidence surface, findings index, and friction, while the user explicitly allowed writing only three paths and named no separate findings-index file. I placed the findings index inside the evidence file after the coverage note, matching the local S42 output convention and respecting the three-file constraint.
- Prompt/source friction: the prompt says it is self-contained and asks me to read the full prompt, but the embedded bundle is a very large single-line JSON block that terminal output truncates. I read the full task/spec sections from the prompt and queried the exact named bundle file `bundles/s042/42_46.ayah.json` for ayah-specific fields so the required topic ledger and evidence could be covered without browsing the repository generally.
- Alignment friction: the bundle reports `word_analysis` has 20 orthographic units while QAC has 16 morphological words, with direct word-level joins marked unreliable. I used `word_morpheme_spans` and QAC morpheme refs for Arabic surfaces and morphology rather than silently correcting upstream identities.
- Evidence friction: HFT later-context deltas and outliers provide refs, roots, branch IDs, roles, and changed readings, but not full Arabic surfaces for every later-ayah word. Where prose used later context, it stayed anchored in this ayah's own words and cited later ayahs by reference instead of inventing later Arabic spans.
- Lexicon friction: `root_001332` (`ك و ن`) and `root_001684` (`و ل ي`) have dictionary branches but no reviewed Turkish gloss in `root_lexicon.coverage`; the prose uses included dictionary branch definitions and records the missing gloss in coverage.
- Channel scope friction: two channel subchannels are anchored at 42:46, but the bundle labels channel review as first-pass discovery rather than an adjudicated channel ledger or maturity plan. I used them only as local candidate support already grounded in `يُضْلِلِ` and `سَبِيلٍ`, and did not name or assert a surah-wide channel.
- Coverage limitation: `v12_reader_responses` are absent under the retired per-ayah workflow, and no whole-surah reading exists for S42. HFT, regular reader walk, wide reader walk, cross-run publication, inter-ayah rows, channel review snippets, root lexicon, and branch inventories are present and used within their stated limits.
- Style audit: the prose uses explicit negation because the ayah itself is built from repeated negated availability frames. Additional correctional negation was kept for likely Turkish-reader misconceptions: `awliyāʾ` as casual friends, `sabil` as only a physical road, and human mediation as erased by the denial of independent patrons.
