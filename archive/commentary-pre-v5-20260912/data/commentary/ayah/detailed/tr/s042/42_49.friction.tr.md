=== PROMPT FRICTION ===

- Output contract friction: the prompt asks for prose, evidence surface, findings index, and friction, while the user explicitly allowed writing only three paths and named no separate findings-index file. I placed the findings index inside the evidence file after the coverage note, following the existing S42 output convention and respecting the three-file write constraint.
- Prompt/source friction: the prompt says it is self-contained and not to read other files, but the embedded bundle is a very large single-line JSON block that the terminal truncates. I read the full task/spec sections from the prompt and queried the exact named bundle file `bundles/s042/42_49.ayah.json` for ayah-specific fields so the topic ledger and evidence could be covered.
- Alignment friction: the bundle reports `word_analysis` has 20 orthographic units while QAC has 15 morphological words, with direct word-level joins marked unreliable and dangling upstream refs for `42:49:16` through `42:49:20`. I used `word_morpheme_spans` and QAC morpheme refs for Arabic surfaces and morphology rather than silently correcting upstream identities.
- Grounding friction: HFT later-context deltas use several later ayahs as activation context but often provide conceptual roles rather than full Arabic surfaces for those later words. I cited later context by reference and kept the prose entry through this ayah's own words: `مُلْكُ`, `يَخْلُقُ`, `يَشَآءُ`, `يَهَبُ`, `إِنَٰثًۭا`, and `ٱلذُّكُورَ`.
- Channel scope friction: two channel subchannels are anchored at 42:49, but the bundle labels channel review as first-pass discovery rather than an adjudicated channel ledger or maturity plan. I used them only as local candidate support where already grounded in this ayah's words and did not name or assert a surah-wide channel in prose.
- Coverage limitation: `v12_reader_responses` are absent under the retired per-ayah workflow, and no whole-surah reading exists for S42. HFT, regular reader walk, wide reader walk, cross-run publication, inter-ayah rows, root lexicon, branch inventories, and anchored channel subchannels are present and used within their stated limits.
- Style audit: explicit negative predicates were kept where they protect containment: lām of ownership versus lām of recipient, literal sex terms versus distant material resonances, and gift/reception versus entitlement or hierarchy. The ayah's own discourse required those corrections, but the main prose frame remains positive: ownership, creation, will, gift, recipient, and paired objects.
