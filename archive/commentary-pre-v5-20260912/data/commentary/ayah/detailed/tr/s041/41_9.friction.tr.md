=== PROMPT FRICTION ===

Kullanıcı yalnız üç dosya yazılmasını istedi; prompt ise Layer 2 sözleşmesinde ayrıca findings index talep ediyor. Ayrı bir `index` dosyası yazmak kullanıcının “write only these files” kısıtını ihlal edeceği için bulgular dizini evidence dosyasına ayrı bölüm olarak kondu.

Prompt dosyası self-contained olduğunu söylüyor, fakat bundle JSON tek satır ve çok büyük olduğu için terminal çıktısı truncation üretti. Aynı named bundle verisi `bundles/s041/41_9.ayah.json` üzerinden hedefli `jq` okumalarıyla incelendi; bunun zorunlu olup olmadığı promptta açık değildi.

`word_analysis` 17 ortografik/analitik birim sayarken QAC 14 morfolojik kelime sayıyor ve coverage bunu upstream alignment defect olarak kaydediyor. Bu nedenle kanıtta `word_morpheme_spans` esas alındı; hiçbir QAC kimliği yeniden numaralanmadı.

HFT üç outlier verirken profil “özellikle surprising outlier'ları koru” diyor; iki outlier keşifsel ve prose içinde zaten daha yakın delille taşınan payoffları yineliyordu. `outlier-encompassed-peer` alındı; `outlier-arrested-fruit-sheath` ve `outlier-opposition-as-transformable-status` ayrı prose/index okuması yapılmadı.

Negation style audit: Prose içinde bir açık olumsuz düzeltme kullanıldı. Bu, `lam`ın ayrı yemin çerçevesi sanılması riskini sınırlamak için gerekli görüldü; izin verilen tavan aşılmadı.
