=== PROMPT FRICTION ===

- `word_analysis` 37 analitik/ortografik kelime kullanırken QAC 30 morfolojik kelime kullanıyor; son yedi `aligned_qac_word_ref` QAC'ta bulunmuyor. Paket bunu açıkça upstream kusuru sayıyor ve `word_morpheme_spans` ile güvenilir bir çapraz geçiş veriyor. Topic kimliklerini değiştirmedim, QAC atıflarını yalnızca bu span kayıtlarından kurdum.
- Genel görev açıklaması, ölçülmüş örneklerde `narrowed` konuların tümünün `candidate` olduğunu söylüyor; bu ayetin bundle'ında 16 `narrowed` konu açıkça `must_integrate`. Ayete özgü `commentary_obligation` alanını yetkili kabul edip on altısının tamamını taşıdım.
- “Your response” bölümü üç parça isterken ana çıktı sözleşmesi bir findings index de istiyor ve orkestratör dört ayrı dosyayı zorunlu kılıyor. Orkestratörün dört dosyalı sözleşmesini izledim.
- HFT kapsamı `split_root_count: 0` bildirirken `root_lexicon` bu ayette dört split kök kaydediyor; HFT'nin `outlier-demolition-guidance` kaydı ayrıca hidayet için baskın olmayan hedefi açıkça kullanıyor. Uç okumada HFT'nin kendi `mapped_root_id + branch_id` atfını ve `root_lexicon` eşlemesini birlikte esas aldım.
- HFT bağlam değişimleri başka ayetlerin ref, kök, kelime sırası ve rolünü veriyor, fakat çoğunda tam Arapça yüzeyi vermiyor. Görev ise başka ayete dayanan her okumada o ayetin kelimesi için tam `{ar, tr, gloss}` span şartı koyuyor. Bu iki talep birlikte bütün on bağlam değişimini yayımlamayı imkânsızlaştırdı; yalnızca yüzeyi bundle içinde aynen doğrulanabilen tekrarları ve kanal anchor'ında açıkça verilen 41:39 kelimelerini kullandım, kalanları coverage kontrolünde tuttum.
- Kök düzeyindeki fallback envanter `ن د و:B002`yi yükseltilmiş çağrı olarak numaralarken QAC-Furuq eşlemeli tam Türkçe sözlük baskın `root_001487` içinde çağrıyı `B001`, menzili `B002`, nemi `B005`, cömertliği `B008` olarak veriyor. Delil yüzeyinde çıplak kök-dal yerine HFT ve `root_lexicon` içindeki `root_id:branch_id` kimliklerini kullandım.

Stil denetimi: Prose'da üç açık olumsuz düzeltme kullanıldı. Üçü de HFT uç okumalarının yerel sözlük anlamını devralma riskini engellemek için “bu dal yerel anlam değildir” sınırını kuruyor; profil tavanı aşılmadı ve başka retorik olumsuzluk eklenmedi.
