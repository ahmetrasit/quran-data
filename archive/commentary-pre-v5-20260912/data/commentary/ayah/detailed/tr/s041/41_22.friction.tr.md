=== PROMPT FRICTION ===

- The prompt requires a findings index, but the user allowed exactly three output files and did not provide a separate findings path. I placed the findings index inside the evidence file under its own heading to preserve the three-file limit.
- The word_analysis `aligned_qac_word_ref` values are flagged by bundle coverage as unreliable because orthographic word count and QAC word count diverge. I used `word_morpheme_spans` and qac morphemes for surface/morphology joins, and cited word_analysis topic IDs for reading obligations.
- The prompt says to read the full prompt file and also says the prompt is self-contained. The inlined bundle is too large for a single tool return and was truncated in display, so I read the full prompt in chunks and used the local bundle file named by the prompt source list for targeted extraction of the same inlined data.
- Later-context material was required by the rendering profile through HFT, but the prose also had to keep at least two-thirds of attention on this ayah's own wording. I compressed later context into two paragraphs and grounded it through explicit later ayah words where used.
- Style audit: explicit Turkish negative predicates were kept only where they protected the reader from mistaking embedded reported negation for the ayah's own assertion or where the Arabic negative surface itself was being explained. No additional negative correction was needed.
