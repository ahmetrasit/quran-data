=== PROMPT FRICTION ===

- The task specification requires a separate findings index, while the user authorized exactly three output files: prose, evidence, and friction. I placed the findings index inside the evidence file under its own heading and did not create a fourth file.
- The prompt says the instantiated prompt is self-contained by default, but later-context grounding requires exact Arabic spans for later ayah words before using HFT deltas. The bundle names the exact HFT packet source file, so I read only that named packet to recover the necessary later surfaces.
- The profile says to include every `candidate` with a payoff not already expressed, but payoff overlap is not mechanically defined. I admitted all 24 candidates because each had a distinct grammar, sound, form, context, or reader effect; repeated material was compressed in prose.
- `ثَمُودَ` has proper-name obligations but no QAC root, while the coverage root list still names roots only from QAC. I grounded Semûd through word_analysis and QAC proper-noun morphology, not root_lexicon.
- HFT outliers are explicitly exploratory. I included the Âd-return and side-to-breadth readings only as bounded echoes, kept the proper-name readings primary, and marked the outlier claims as inference.
- Style audit: the prose uses no authorial explicit negative predicates. One later-word span glosses 41:26 `لَا تَسْمَعُوا۟` as “dinlemeyin” because the cited Arabic word itself is a negative imperative; other contrastive risks were rewritten as positive predication, and no sentence stacks two negatives.
