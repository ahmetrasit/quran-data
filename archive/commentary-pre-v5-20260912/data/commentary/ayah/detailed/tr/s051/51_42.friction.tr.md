=== PROMPT FRICTION ===

The run was mostly clean.

Two points required judgment. First, the prompt says Layer 2 carries the full field, while the 5.6 profile says to include candidates with reader payoff not already expressed. I treated "include" as satisfied when a candidate's payoff was integrated into a connected sentence rather than repeated as its own paragraph.

Second, the bundle both inlines first-pass channel material and lists external generated channel files. Because State B forbids established channel claims and the local channel relation was already present in the inlined review, I did not read the external generated files.

`v12_reader_responses` and `v12_focus_trace_hermetic` were absent. I reported that in the evidence coverage note and did not infer staged reader movement.
