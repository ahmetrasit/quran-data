=== PROMPT FRICTION ===

The run was mostly clean. The main friction was the bundle's known word-analysis/QAC alignment defect: word_analysis uses seven analytic words while QAC has five morphological words, leaving dangling refs for later word_analysis indices. I handled this by treating word_analysis topic ids and word_morpheme_spans as the stable bridge, and I recorded that in the evidence surface.

The prompt asks for before/after value from staged reader responses or Hermetic Focus Trace when present, but both are absent here. Reader walks and cross-run publication are present, so I used them only as retrospective/contextual signals, following the V2 profile.

The channel material is useful but explicitly first-pass and not a ledger. I did not read external generated channel files because the inlined anchored review supplied enough local detail, and the user asked me to work from the assigned prompt and write only the three assigned files.

I kept explicit negative constructions within the profile's ceiling and used them for boundary work: the implied addressee and objectless command are grammatical facts, and the document branch needed replacement-risk control. No additional rule had to be invented.
