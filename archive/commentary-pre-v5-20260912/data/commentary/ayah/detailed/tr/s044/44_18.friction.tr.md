=== PROMPT FRICTION ===

- Orchestrator dört ayrı dosya isterken promptun son “Your response” bölümü çıktıyı üç parça diye sayıyor ve findings index’i bu listede anmıyor. Üstteki bağlayıcı output contract ile açık dosya listesi index’i zorunlu kıldığı için dört dosya yazıldı.
- `ع ب د` için scoped branch inventory `B001` ve `B003-B012` dallarını taşırken tam `root_lexicon` ayrıca `root_000973/B002` (“Tanrı’ya ait kul”) dalını taşıyor. İki kaynak arasındaki dal-seti farkı için öncelik kuralı verilmemiş. Yerel aidiyet iddiasında zorunlu word-analysis konuları esas alındı; `B002` bağımsız findings-index okuması yapılmadı.
- HFT, `أَدُّوا` için morfolojik bakımdan uzak ve baskın olmayan `root_001634/B001` sevgi eşlemesini “surprising_valid_outlier” olarak koruyor; prompt ise adayların okuyucu getirisine göre alınmasını ve primary okumanın korunmasını istiyor. Okuma, çeviri alternatifi yapılmadan açıkça uzak bir aidiyet-onarımı gölgesi olarak alındı.
- HFT’nin `root_000021/B009` peş peşe ölüm okuması da geçerli aykırı okuma diye işaretlenmiş, fakat yerel fiil anlamına çok uzak. Prompt bunu zorunlu mu aday mı sayacağını açıkça belirtmiyor. Okuma, yalnız 44:24’teki boğulma kelimesiyle zeminlenmiş sonuç yankısı olarak alındı ve fiilin yerel teslim anlamı cümle içinde korundu.
- Evidence surface için zorunlu alanlar belirtiliyor, fakat dosya şeması tanımlanmıyor. Adreslenebilir bir ifade tablosu, aday tasarrufu ve coverage bölümü kullanıldı.
