=== PROMPT FRICTION ===

## Editorial pass

Bu dosya seti yalnızca ilk geçişin editoryal yeniden yazımıdır. İlk geçişteki dört dosya değiştirilmedi. Somut bir iç tutarsızlık bulunmadığı için bulgular, anchor’lar, karşı deliller, eksik bundle kayıtları ve hüküm sınırları aynen korundu.

## Editoryal kararlar

- Prose, ilk geçişte art arda yinelenen aynı dīn açıklamalarını tek bir anlam hareketinde topladı; bā, belirli tanımlık, borç/hesap, bağlılık/düzen ve causal/exposure baskıları korunarak P07-P08’e dağıtıldı.
- Kelime sırası, cümle kuruluşu ve ses gözlemleri önce; context deltas ve split-root analojileri sonra getirildi. Böylece reader ground aynı kaldı, fakat ilerleme daha az kataloglayıcı hâle geldi.
- `cognitive-seeing-channel` + `recognition-confronts-denial`, `seeing-versus-display` + `recognition-and-display-frame` + `delta_1076_gaze_reversal`, `secondary-causal-nuance` + `polyvalent-accountability` ve `sound-doubling` + `final-sound-landing` ortak mekanizma/payoff nedeniyle ortak prose hareketlerinde tutuldu. Her biri index’te ayrı satır ve evidence’te ayrı ref olarak kaldı.
- `delta_1073_distributed_provision` ile `delta_1073_social_graft`, ayrıca `delta_1077_blocked_aid_circuit` ile `delta_1077_fortified_possession`, aynı paragrafta fakat ayrı cümle inişleriyle taşındı. Bu, yorumları birleştirmeden okuma akışını sıkılaştıran editoryal bir karardır.
- Dört baseline model ayrı ranked resonance yapılmadı; ilk geçişteki gibi kelime landings ve context resonance’lara bağlandı. Dokuz delta ve üç outlier toplam 12 `surprise:` satırı olarak korundu.
- İlk geçişte korunan cross-run `reject` satırı ritual-counterfeit bulgusuna karşı delil olarak kaldı. Exploratory fire, graft, reputation, star ve quenching görüntüleri analoji sınırlarıyla yazıldı; hiçbiri birincil anlamın yerine geçirilmedi.
- Editorial prose iki açık olumsuzlama ile sınırlandı: yıldız görüntüsünün namazın sözlük anlamı sayılmaması ve `ara'ayta`nın su taşımak diye çevrilmemesi. Bunlar muhtemel yanlış anlamayı önleyen containment cümleleridir.

## Bundle sınırlılıkları

- `v12_reader_responses` yok; strict per-ayah staged workflow emekliye ayrıldığı için stage transcript veya confidence movement eklenmedi.
- `channel_generated_outputs` şu dosyayı eksik bildiriyor: `paths/path_families/semantic_path_families.jsonl.gz`. Inline channel/HFT malzemesi yeterli olduğu için dış dosya okunmadı.
- `root_001290` (`ك ذ ب`) için reviewed Turkish gloss yok. Dictionary branches, QAC morphology, word analysis ve activation kayıtları korunarak denial reading sürdürüldü.
- Occurrence tablolarının `occurrences` ve `ayahs` alanları bundle tasarımı gereği trim edilmiş. No Layer 1 `consideredNotPrimary` artifact is present; absent exclusion reading was not invented.
- Word morpheme spans dört kelimenin dördünde mevcut; unresolved hizalama kaydı yok.

## Density audit

- `must_integrate` topic sayısı: **9**.
- Admitted `candidate` topic sayısı: **14**.
- Distinct resonance sayısı: **12** (9 context delta + 3 surprising outlier).
- Editorial prose resonance landing sayısı: **12**.
- Shared landings: P02/P09 (`cognitive-seeing-channel` + `recognition-confronts-denial`), P15 (`seeing-versus-display` + `recognition-and-display-frame` + `delta_1076_gaze_reversal`), P08 (`secondary-causal-nuance` + `polyvalent-accountability`), P09 (`sound-doubling` + `final-sound-landing`), P11 (distributed provision + social graft) and P18 (blocked aid circuit + fortified possession). Her paylaşım ortak mekanizma ve reader payoff gerekçesine dayanır; ayrı findings rows ve evidence refs korunmuştur.
